# SEEKR Goal Status

Last updated: 2026-05-11.

## Objective

Finish SEEKR as a plug-and-play internal-alpha system: a local-first GCS with AI implemented through the local Ollama operator path, simulator/SITL flight core, replay/evidence flow, read-only drone integration, startup preflight, and review bundle evidence so an operator can install, start, and validate the local system with minimal setup. This remains an internal demo and audit system, not production flight firmware.

The aircraft safety rule is still absolute: real MAVLink, ROS 2, PX4, ArduPilot, geofence, mission upload, mode-change, arm, takeoff, land, return-home, terminate, and waypoint command paths stay blocked outside simulator/SITL transports.

## Prompt-To-Artifact Checklist

| Requirement | Artifact or evidence | Current status |
| --- | --- | --- |
| Local-first GCS opens and exposes mission, map, detections, source health, readiness, replay, and exports | `src/client/*`, `tests/ui/gcs-smoke.pw.ts`, `docs/SEEKR_GCS_ALPHA_TODO.md` | Implemented and covered by Playwright smoke |
| Event-sourced mission state and hash-chain verification | `src/server/eventLog.ts`, `src/server/persistence/eventStore.ts`, `src/server/__tests__/eventSourcing.test.ts`, `/api/verify` | Implemented and covered by Vitest/API tests |
| Startup hash-chain restore failures are fail-closed with bounded diagnostics | `src/server/index.ts`, `src/server/startupLog.ts`, `src/server/__tests__/startupLog.test.ts` | Implemented; tampered persisted logs are still rejected, but startup logs summarize the error count, first errors, and truncated count instead of dumping unbounded validation arrays |
| Replay export/reload/seek verifies event count and final hash | `src/server/persistence/replayStore.ts`, `src/server/__tests__/persistenceReport.test.ts`, `src/server/__tests__/simulatorReplay.test.ts` | Implemented and tested |
| Evidence/reporting/incident logs work end-to-end | `src/server/report.ts`, `src/server/domain/incidentLog.ts`, `tests/ui/gcs-smoke.pw.ts`, `docs/V1_ACCEPTANCE.md` | Implemented and tested |
| Production preview artifact is smoke-tested | `scripts/preview-smoke.ts`, `package.json` `smoke:preview`, acceptance script contract test | Implemented; acceptance now runs preview smoke |
| Field-laptop install integrity can be archived | `scripts/release-checksums.ts`, `package.json` `release:checksum`, `.tmp/release-evidence/`, `.gitignore`, `.npmrc` | Implemented; writes JSON, SHA-256, and Markdown checksum evidence without claiming hardware validation, and includes repository-safety files so `.gitignore` and npm `engine-strict` policy are covered by the release hash |
| Package, runtime, release, and acceptance versions agree | `package.json`, `package-lock.json`, `src/shared/constants.ts`, `src/server/__tests__/acceptanceScripts.test.ts` | Implemented; package version is aligned with `SEEKR_SOFTWARE_VERSION` so release evidence filenames, `/api/session`, and acceptance status report the same build version |
| Local rehearsal API state can be archived for operator notes | `scripts/rehearsal-evidence.ts`, `package.json` `rehearsal:evidence`, `.tmp/rehearsal-evidence/` | Implemented; snapshots read-only API state and safety boundary without claiming hardware validation; optional `--require-source` fails real bench snapshots when named MAVLink/ROS/LiDAR sources are missing or eventless |
| Fresh-operator rehearsal notes can be prepared without claiming completion | `scripts/rehearsal-note.ts`, `package.json` `rehearsal:note`, `.tmp/rehearsal-notes/` | Implemented; generates fill-in JSON/Markdown notes with `freshOperatorCompleted: false` until a human fills required fields |
| Filled fresh-operator rehearsal closeouts can be validated before completion is claimed | `scripts/rehearsal-closeout.ts`, `package.json` `rehearsal:closeout`, `.tmp/rehearsal-notes/seekr-rehearsal-closeout-*` | Implemented; requires operator fields plus valid before/after evidence snapshots before `freshOperatorCompleted: true` |
| HIL failsafe/manual override evidence can be archived without claiming fixture evidence as real | `scripts/hil-failsafe-evidence.ts`, package.json `hil:failsafe:evidence`, `.tmp/hil-evidence/` | Implemented; requires actual target-board hardware evidence, valid rehearsal/source evidence, non-empty flight log, manual override, E-stop, and `commandUploadEnabled: false` before completion, rejects referenced evidence/log paths that escape the project root, and completion audit rechecks those referenced files before clearing the blocker |
| Isaac Sim to Jetson HIL capture evidence can be archived without claiming local fixtures as real | `scripts/isaac-hil-capture-evidence.ts`, package.json `isaac:hil:evidence`, `.tmp/isaac-evidence/` | Implemented; requires actual Jetson hardware evidence, Isaac source-health evidence, positive capture manifest counts, non-empty capture log, and `commandUploadEnabled: false` before completion, rejects referenced evidence/log paths that escape the project root, and completion audit rechecks those referenced files before clearing the blocker |
| Future hardware-actuation policy review can be validated without enabling runtime commands | `scripts/hardware-actuation-policy-gate.ts`, package.json `policy:hardware:gate`, `.tmp/policy-evidence/` | Implemented; requires acceptance, actual target-board evidence, completed HIL evidence, two reviewers, manual override/E-stop requirements, and false authorization fields before a package is only `ready-for-human-review`, rejects referenced policy/evidence/review-packet paths that escape the project root, and completion audit rechecks referenced policy, acceptance, hardware, and HIL evidence before clearing the blocker |
| Static command-boundary regressions are caught before acceptance | `scripts/command-boundary-scan.ts`, `package.json` `safety:command-boundary`, `.tmp/safety-evidence/`, `src/server/acceptanceEvidence.ts` | Implemented; scans source/scripts for unsafe true command flags, real-adapter accepted commands, outbound command tokens outside SITL mapping, missing real-adapter command rejection, and is recorded as a required acceptance evidence command with the latest scan summary embedded in acceptance status |
| Final API evidence readback is persisted | `scripts/api-probe.ts`, `package.json` `probe:api`, `.tmp/api-probe/` | Implemented; writes JSON/Markdown evidence that `/api/session` read back acceptance release checksum and command-boundary scan summaries after `acceptance:record` |
| Completion audit separates local alpha readiness from real hardware blockers | `scripts/completion-audit.ts`, `package.json` `audit:completion`, `.tmp/completion-audit/` | Implemented; returns local alpha consistency only when acceptance matches the latest release checksum, command-boundary scan, and API-probe evidence, keeps `complete: false` until real-world evidence exists, emits separate actual Jetson Orin Nano and Raspberry Pi 5 blocker items plus machine-readable `realWorldBlockerIds`, and requires actual target-board archives with `actualHardwareValidationComplete: true` and `hardwareValidationScope: "actual-target"`, live bridge-run evidence, and required-source rehearsal evidence before clearing real MAVLink/ROS blockers |
| TODO audit keeps planning docs aligned with real-world blockers | `scripts/todo-audit.ts`, `package.json` `audit:todo`, `.tmp/todo-audit/`, `src/server/__tests__/todoAudit.test.ts` | Implemented; extracts unchecked TODOs from `docs/SEEKR_GCS_ALPHA_TODO.md` and `docs/SEEKR_COMPLETION_PLAN.md`, compares them to the current completion-audit blocker categories, lets a category pass only when both its completion blocker and unchecked TODO are cleared, separates real-world blocker, blocked category, and validation blocker counts in JSON and CLI output, requires the latest completion audit artifact to match, requires review-bundle checks to preserve exact category ordering, and keeps `commandUploadEnabled: false` |
| Local plug-and-play setup can prepare operator files without overwriting edits | `scripts/local-setup.ts`, `package.json` `setup:local`, `.tmp/plug-and-play-setup/`, `src/server/__tests__/localSetup.test.ts` | Implemented; creates `.env` from `.env.example` only when missing, prepares `.tmp/rehearsal-data`, refuses env/data paths outside the project root, writes JSON/Markdown setup evidence, keeps command upload and hardware actuation disabled, leaves Ollama model installation plus real hardware validation to explicit later checks, and is required by plug-and-play readiness, review-bundle creation, copied-bundle verification, and goal audit to be no older than the latest acceptance record |
| Local AI model preparation has first-class evidence | `scripts/local-ai-prepare.ts`, `package.json` `ai:prepare`, `.tmp/local-ai-prepare/`, `src/server/__tests__/localAiPrepare.test.ts` | Implemented; runs bounded `ollama pull llama3.2` by default, supports custom local Ollama model setup, writes JSON/Markdown evidence with `commandUploadEnabled: false`, fails closed on missing model prep, and is required by operator quickstart, rehearsal wrapper, doctor, plug-and-play readiness, review bundle, bundle verification, and goal-audit contracts. Plug-and-play readiness, bundle creation, and copied-bundle verification now require the prepared model and pull command to match the latest acceptance strict local AI model, and the shared artifact validator rejects passing local-AI prepare evidence unless the command is exactly three tokens, `ollama pull <model>` or an absolute `ollama` executable path plus `pull <model>`, with a non-option Ollama model token using only letters, numbers, dots, underscores, colons, slashes, or hyphens, generatedAt no older than the latest acceptance record, and artifact check evidence containing the exact command string; relative lookalike executable paths, shell-metacharacter model arguments, and stale prepare artifacts are rejected |
| Source-control handoff can be audited without publishing or claiming hardware readiness | `scripts/source-control-handoff.ts`, package.json `audit:source-control`, `.tmp/source-control-handoff/`, `src/server/__tests__/sourceControlHandoff.test.ts` | Implemented; writes read-only JSON/Markdown evidence and CLI summary fields for package/README GitHub references, the parent GitHub landing README ordered fenced shell command lines for a fresh-clone path into `SEEKR/software`, bounded smoke followed by a final standalone doctor rerun before strict local AI smoke and plug-and-play audit guidance, shallow fresh-clone contents for the landing README, package manifest, lockfile, env template, rehearsal start wrapper, operator quickstart, cloned landing README contract proof, shared operator-quickstart contract proof, and `npm ci --dry-run` install consistency, local Git metadata, configured remotes, local branch, GitHub remote refs/default branch via `git ls-remote`, local HEAD versus GitHub default branch SHA, clean-worktree status, exact ordered source-control check IDs, persisted blocked/warning check counts, and manual publication next-action steps for missing or misordered GitHub landing guidance, incomplete fresh-clone contents, invalid published landing README or operator quickstart guidance, `.git`, origin, refs, unpublished HEAD, dirty worktrees, and re-audit, while refusing to initialize Git, commit, push, change GitHub settings, validate hardware, or enable command upload; exports a shared semantic validator used by doctor, plug-and-play readiness, bundle creation, and bundle verification so publication gaps can remain warnings only after artifact shape, repository URL, exact required checks, check counts, next-action shape, limitations, disabled command upload, ready-state remote URL/local-branch/default-branch/ref-count/local-head-to-remote-SHA/worktree consistency, `github-landing-readme` pass evidence for ordered fenced shell command-line proof through bounded smoke, final doctor rerun, strict local AI smoke, and plug-and-play audit plus non-negated explicit disabled/blocked/locked/false command-upload and hardware-actuation wording plus the distinct final AI/readiness marker, and `fresh-clone-smoke` pass evidence for the GitHub URL, exact clone command, exact npm dry-run command, cloned landing README contract proof, shared operator-quickstart contract proof, and all startup-critical `fresh-clone:` files are verified |
| Plug-and-play readiness can be audited without clearing hardware blockers | `scripts/local-setup.ts`, `scripts/local-ai-prepare.ts`, `scripts/plug-and-play-doctor.ts`, `scripts/plug-and-play-artifact-contract.ts`, `scripts/plug-and-play-readiness.ts`, `scripts/operator-quickstart-contract.ts`, `scripts/rehearsal-start.sh`, `scripts/rehearsal-start-smoke.ts`, `scripts/fresh-clone-operator-smoke.ts`, `docs/OPERATOR_QUICKSTART.md`, `.gitignore`, `.npmrc`, package.json `setup:local`/`ai:prepare`/`doctor`/`plug-and-play`/`rehearsal:start`/`smoke:rehearsal:start`/`smoke:fresh-clone`/`audit:plug-and-play`, `.tmp/plug-and-play-setup/`, `.tmp/local-ai-prepare/`, `.tmp/plug-and-play-doctor/`, `.tmp/rehearsal-start-smoke/`, `.tmp/fresh-clone-smoke/`, `.tmp/plug-and-play-readiness/`, `src/server/__tests__/localSetup.test.ts`, `src/server/__tests__/localAiPrepare.test.ts`, `src/server/__tests__/plugAndPlayDoctor.test.ts`, `src/server/__tests__/plugAndPlayArtifactContract.test.ts`, `src/server/__tests__/plugAndPlayReadiness.test.ts`, `src/server/__tests__/freshCloneOperatorSmoke.test.ts`, `src/server/env.ts`, `src/server/__tests__/envLoader.test.ts` | Implemented; adds a non-destructive local setup command, first-class local AI model preparation evidence, runnable local doctor preflight, an obvious `npm run plug-and-play` alias for the checked `rehearsal:start` wrapper, and published fresh-clone operator smoke proof for package scripts, package `engines`/`packageManager`, Node/package-lock readiness, repository safety via `.gitignore` protection for local secrets, generated artifacts, and runtime `data/` plus `.npmrc` `engine-strict`, latest source-control handoff artifact semantics for the GitHub reference with missing Git metadata, GitHub refs, unpublished local HEAD, or dirty worktree state preserved as source-control audit warnings but failing plug-and-play readiness unless the handoff is warning-free and semantically valid, local `tsx`/`concurrently`/`vite` dependency binaries needed by `npm run dev`, rehearsal-start wrapper wiring/defaults/API-port normalization/free-port fallback/setup-before-ai-prepare-before-source-control-before-doctor-before-dev ordering so stale source-control handoff artifacts are refreshed before startup, bounded rehearsal-start smoke evidence with exact ordered rows and artifact pointers for setup/ai-prepare/source-control/doctor/API/client/source-health/readiness/shutdown, fresh-clone operator smoke evidence for GitHub clone/install/operator-start/strict-local-AI/final-doctor proof with strict-AI smoke path/provider/model/loopback URL/case-count summaries, env setup, Ollama model availability, API/client port availability, healthy already-running SEEKR listener recognition, auto-recoverable unconfigured default-port conflict recognition with listener diagnostics, explicit occupied-port warnings, data directory safety, and unsafe command-upload/hardware-actuation environment flags, then verifies the package command surface, source-control handoff artifact status as a warning-free publication gate with semantic validation and a ready-artifact generatedAt not older than acceptance, plug-and-play rehearsal start wrapper with project-local data, expected read-only source defaults, API/client port normalization, unconfigured-default free-port fallback, and setup-before-ai-prepare-before-source-control-before-doctor-before-dev ordering, operator quickstart coverage through a shared contract for GitHub clone plus `cd SEEKR/software`, `npm ci` before setup, setup/ai:prepare/source-control audit/start, occupied-port recovery, bounded smoke, strict local AI smoke proof, advisory-only Ollama AI that cannot create command payloads or bypass validation, API evidence, source-health, real-world blockers, and non-negated, non-contradictory disabled command/hardware authority, `.env.example` operator defaults for local ports, data directory, env-file loading controls, Ollama provider, URL, model, and timeout, project-local env loader implementation/wiring/tests, built production shell, latest acceptance/release/API probe evidence including session-visible timestamp, exact required local-AI strict smoke scenario names, release checksum, and command-boundary scan summaries matched to acceptance, local AI prepare model/pull-command matching against the latest acceptance strict local AI model, strict local Ollama AI smoke with saved per-case plan-kind, validator-pass, unsafe-operator-text, and mutation evidence, current gstack QA/workflow status with exact workflow and perspective rows, review bundle verification including copied setup, local AI prepare freshness, operator-start doctor, exact-row rehearsal-start smoke, fresh-clone operator smoke, strict local AI smoke status, and source-control handoff semantics, current review-bundle pointers to the latest handoff bundle/gstack workflow/gstack QA/TODO audit/source-control handoff/plug-and-play setup/local AI prepare/plug-and-play doctor/rehearsal-start smoke/fresh-clone smoke/strict local AI smoke status evidence plus packaged operator quickstart evidence, exact ordered setup artifact rows, exact local AI prepare model-prep evidence, exact ordered operator-start doctor artifact rows, exact ordered rehearsal-start smoke artifact rows, exact fresh-clone operator smoke artifact rows, and critical-versus-soft doctor check statuses through shared setup/operator-start/fresh-clone artifact contracts including explicit repository-safety/source-control-handoff evidence plus runtime dependency evidence for `package.json` engines, `packageManager`, `package-lock.json`, `node_modules/.bin/tsx`, `node_modules/.bin/concurrently`, and `node_modules/.bin/vite`, explicit audit coverage that the doctor source/test contract preserves healthy occupied-port recognition, treats unconfigured default conflicts as pass when rehearsal-start free-port fallback is available, surfaces explicit occupied-port soft warnings and auto-recoverable default-port fallback dependencies in plug-and-play readiness, and records listener diagnostics for occupied non-SEEKR ports, with the shared doctor artifact contract rejecting non-SEEKR port warnings when listener diagnostics are dropped and auto-recoverable port passes when fallback candidate proof is dropped, latest operator-start doctor artifact freshness relative to acceptance and the latest source-control handoff, persisted real-world blocker count/ID/list agreement, and completion-audit safety boundary, while keeping `complete: false` and listing real-world blockers until physical evidence exists |
| Plug-and-play readiness publishes reviewable source-control, port, fresh-clone, and bundle-proof state | `scripts/plug-and-play-readiness.ts`, `.tmp/plug-and-play-readiness/`, `src/server/__tests__/plugAndPlayReadiness.test.ts`, `src/server/__tests__/goalAudit.test.ts` | Implemented; the readiness JSON, Markdown, and CLI output now expose latest source-control handoff status, repository URL, package repository URL, configured remotes, local branch, remote default branch, remote ref count, blocked/warning check counts, local HEAD, GitHub default-branch SHA, source-control shallow fresh-clone HEAD, source-control npm dry-run result, source-control checked startup-file count, clean-worktree flag, working-tree status-line count, operator-start doctor port status, default-port occupancy, auto-recoverable fallback status, listener diagnostics, fresh-clone operator smoke status, repository URL, local and cloned HEAD, fresh-clone source-control local HEAD, fresh-clone source-control remote default SHA, fresh-clone source-control shallow fresh-clone HEAD, fresh-clone source-control npm dry-run result, fresh-clone source-control checked startup-file count, local AI model, fresh-clone strict-AI smoke path/provider/model/loopback URL/case count, downstream artifact statuses, exact checked rows, review-bundle verification path, checked-file count, secret-scan status, bundled source-control summary, bundled plug-and-play setup generatedAt/status, and bundled local-AI-prepare, operator-start doctor, rehearsal-start smoke, fresh-clone smoke, strict-AI-smoke, and operator-quickstart proof paths, and fail if the verified review bundle's source-control branch/ref/count/SHA or clean-worktree summary drifts from the latest source-control handoff, if plug-and-play readiness hides the latest doctor local-port diagnostics or fresh-clone summary drift, if review-bundle proof path summaries drift from the latest bundle verification, or if rehearsal-start smoke, fresh-clone smoke, handoff bundle, or bundle verification evidence predates the latest acceptance record |
| Plug-and-play readiness fails closed on completion-audit contradictions | `scripts/plug-and-play-readiness.ts`, `src/server/__tests__/plugAndPlayReadiness.test.ts`, `.tmp/plug-and-play-readiness/` | Implemented; `audit:plug-and-play` only reports complete when local plug-and-play checks pass, the latest completion audit explicitly reports `complete: true`, and the completion audit real-world blocker list is empty; malformed completion artifacts with `complete: false` and an empty blocker list, `complete: true` while blockers remain, mismatched `summary.blocked`, or truncated real-world blocker lists fail the real-world boundary check instead of clearing or under-reporting hardware readiness |
| Operator quickstart pins local AI model preparation | `docs/OPERATOR_QUICKSTART.md`, `README.md`, `scripts/operator-quickstart-contract.ts`, `src/server/__tests__/operatorQuickstartContract.test.ts` | Implemented; the operator-facing path now explicitly tells a fresh clone to run `ollama pull llama3.2` before strict local AI smoke, and the shared quickstart contract fails if that model-preparation signal is missing from packaged guidance |
| Demo/audit handoff can be packaged without claiming hardware validation | `scripts/demo-readiness-package.ts`, `package.json` `demo:package`, `.tmp/demo-readiness/` | Implemented; packages latest acceptance, release, command-boundary scan, API probe, completion audit, source-control handoff, hardware archive, policy gate, overnight status pointers, false hardware claims, real-world blockers, operator/safety/DX/replay/demo-readiness perspective review, and next-evidence guidance; the DX perspective now reports missing, unsafe, or pre-acceptance GitHub publication state as a gap and uses a clean published source-control handoff as review-readiness evidence only when it is current against acceptance; Jetson Orin Nano and Raspberry Pi 5 archive checklist items remain separate end to end |
| Real-world bench evidence collection can be planned without claiming validation | `scripts/bench-evidence-packet.ts`, `package.json` `bench:evidence:packet`, `.tmp/bench-evidence-packet/` | Implemented; converts demo next-evidence guidance into ordered operator task cards with preconditions, done criteria, runbooks, false authorization fields, and separate target-specific actual-hardware archive cards for Jetson Orin Nano and Raspberry Pi 5, including compatibility splitting for legacy combined target-board checklist items |
| Internal handoff pointer chain can be verified and bundled before review | `scripts/handoff-index.ts`, `scripts/handoff-verify.ts`, `scripts/handoff-bundle.ts`, `scripts/handoff-bundle-verify.ts`, `package.json` `handoff:index`/`handoff:verify`/`handoff:bundle`/`handoff:bundle:verify`, `.tmp/handoff-index/`, `.tmp/handoff-bundles/` | Implemented; validates the latest demo package, bench packet, acceptance, release, completion audit, safety scan, API probe, hardware archive, policy gate, and overnight pointers, verifies acceptance release checksum JSON/SHA/Markdown paths plus file metadata point at the latest release evidence, verifies acceptance and demo package references embed the same latest command-boundary scan and API-probe evidence, verifies API-probe readback of the acceptance timestamp, command count, exact strict local AI scenario names, loopback Ollama URL, release checksum, and command-boundary scan before indexing, records and rechecks linked-artifact SHA-256 digests, requires passing or limitation-only gstack workflow-status with `gstackAvailable: true`, exact ordered workflow and perspective rows, `pass-with-limitations` for limitation-only evidence, preserved manifest-level limitation details including unavailable gstack CLI details, preserved helper-tool root/count/name evidence when the umbrella CLI is unavailable, preserved limitation details for stale or missing health/QA evidence, per-workflow skill availability, Git review evidence paths when Git metadata is present or no-Git limitations when `.git` is absent, health-history status/path, QA report status/path, QA screenshot path preservation, perspective status/score/next-action details, TODO audit artifacts with exact ordered blocker category IDs, matching top-level/category blocker counts, well-formed supporting TODO/blocker match evidence, read-only source-control handoff evidence with local Git/GitHub publication, repository URL, configured remotes, local branch, remote default branch, remote ref count, blocked/warning check counts, local-head, remote default SHA, `workingTreeClean`, and status-line count surfaced at the bundle-manifest top level for review, passing plug-and-play setup evidence, passing local AI prepare evidence from `npm run ai:prepare`, a passing operator-start plug-and-play doctor artifact that is fresh against acceptance and preserves repository-safety, matching source-control handoff, and runtime dependency evidence for `.gitignore`, `.npmrc`, GitHub publication state, `package.json` engines, `packageManager`, `package-lock.json`, `tsx`, `concurrently`, and `vite`, passing exact ordered bounded rehearsal-start smoke evidence, strict local AI smoke proof matching acceptance with loopback Ollama URL plus per-case plan-kind/validator/unsafe-text/mutation safety fields, and operator quickstart coverage for GitHub clone plus `cd SEEKR/software`, setup/ai:prepare/source-control audit/start, strict-AI-smoke proof, advisory-only Ollama AI that cannot create command payloads or bypass validation, API evidence, source-health, real-world blockers, and non-negated, non-contradictory disabled command/hardware authority before bundling, copies verified local artifacts plus the latest gstack workflow-status, referenced local gstack browser QA report and screenshots, TODO audit, source-control handoff, plug-and-play setup, local AI prepare, operator-start plug-and-play doctor, rehearsal-start smoke, strict local AI smoke status, and operator quickstart artifacts into a review bundle, verifies copied bundle file digests, full high-confidence secret-scan coverage, copied acceptance exact strict local AI scenario names and loopback Ollama URL, copied strict local AI smoke status including exact Ollama scenario order, acceptance-matched timestamp/model/loopback URL, per-case plan-kind, validator pass, no unsafe operator-facing text, and no thinking-time mutation, copied API-probe readback of the same acceptance timestamp, command count, scenario names, loopback Ollama URL, release checksum, command-boundary scan, and disabled command upload, copied gstack workflow semantics including helper-tool evidence when the copied umbrella CLI is unavailable, exact workflow/perspective order, installed-skill availability, top-level limitation status and limitation text, unavailable gstack CLI limitation detail, Git review evidence or no-Git review honesty, health-history path preservation, QA report metadata/screenshot paths and limitation details, copied perspective status/score/next-action details, copied gstack QA report/screenshot inclusion plus report-content pass/command-safety, copied TODO audit exact blocker-category order/top-level-count/support-shape semantics, copied source-control handoff semantics plus bundle-manifest repository URL/configured-remotes/local-branch/default-branch/ref-count/blocked-check-count/warning-check-count/local/remote SHA and clean-worktree summary agreement, copied plug-and-play setup env/data/safety semantics, copied local AI prepare model-prep semantics, copied operator-start plug-and-play doctor repository-safety/source-control-handoff/runtime dependency evidence plus start-wrapper/AI/port/data/safety semantics plus freshness against copied acceptance, copied rehearsal-start smoke exact ordered setup/AI/source-control/smoke-doctor/startup/source-health/readiness/shutdown row semantics plus artifact pointers, and copied operator quickstart GitHub clone/software-directory setup/ai:prepare/source-control audit/start/strict-AI-smoke/advisory-AI/API/source-health/safety guidance including no AI-created command payloads or validation bypass, requiring critical operator-doctor checks to pass while allowing only source-control/port/data-directory warnings, and keeps hardware claims and authorization fields false |
| GStack browser QA can be regenerated repeatably | `scripts/gstack-browser-qa.ts`, `package.json` `qa:gstack`, `.gstack/qa-reports/`, `src/server/__tests__/gstackBrowserQa.test.ts` | Implemented; starts the built local production shell with a clean data directory, captures desktop and mobile screenshots, checks `/api/session`, readiness, hash-chain verification, browser console health, and `commandUploadEnabled: false`, validates generated report timestamps before using them in artifact paths, and writes report-only Markdown evidence without clearing hardware blockers |
| GStack health history can be refreshed from package scripts | `scripts/gstack-health-history.ts`, `package.json` `health:gstack`, `~/.gstack/projects/software/health-history.jsonl`, `src/server/__tests__/gstackHealthHistory.test.ts` | Implemented; runs `npm run check`, appends a parser-compatible JSONL entry with timestamp, numeric score, typecheck/test scores, release checksum context, and `commandUploadEnabled: false`, preserves existing entries, and refuses to record health history if acceptance ever reports command upload enabled |
| Interrupted local work can be resumed from a single status artifact | `scripts/local-recovery-status.ts`, `package.json` `status:local`, `.tmp/recovery-status/`, `src/server/__tests__/localRecoveryStatus.test.ts` | Implemented; writes JSON/Markdown from the latest acceptance, API readback, source-control handoff, fresh-clone strict AI proof, review-bundle verification, gstack health/QA, plug-and-play readiness, goal audit, overnight status, fallback-port warning with listener diagnostics/details, and remaining real-world blocker artifacts without rerunning the expensive chain or enabling command upload, and fails closed if acceptance points at stale release JSON/SHA/Markdown evidence, stale command-boundary JSON/Markdown evidence, stale command-boundary counts, stale `/api/session` acceptance readback, strict local AI case names drift, or fresh-clone, plug-and-play, or review-bundle evidence is stale against the current source-control HEAD |
| Evidence artifact paths reject unsafe caller input | `scripts/artifact-paths.ts`, `scripts/acceptance-record.ts`, evidence writer scripts, `src/server/__tests__/artifactPaths.test.ts`, `src/server/__tests__/acceptanceScripts.test.ts` | Implemented; all local evidence writers now validate normalized parseable ISO UTC timestamps before converting them into artifact filename segments, require artifact output directories and referenced input paths to stay under the project root, constrain acceptance recorder output/status/release/safety evidence paths under the project root, and use shared filename-label sanitization for caller-provided label/mode segments, with source contracts that keep `DEFAULT_OUT_DIR` writers on the shared output guard and prevent local sanitizer drift, so supplied `generatedAt`/archive timestamps, `outDir`, label, mode, acceptance status path, release directory, safety directory, or referenced evidence path values cannot create nested, escaped, impossible-date, external, or ambiguous artifact paths |
| GStack workflow status is captured for internal-alpha review | `scripts/gstack-workflow-status.ts`, `package.json` `audit:gstack`, `.tmp/gstack-workflow-status/`, `src/server/__tests__/gstackWorkflowStatus.test.ts` | Implemented; writes machine-readable exact ordered health, review, planning, and QA workflow status with exact ordered operator, safety, DX, replay, and demo-readiness perspective status/score/next-action details, while separately recording per-workflow installed skill availability, `gstack` CLI availability, local gstack helper-tool root/count/name evidence when helper tools are installed without the umbrella CLI, parent/local Git metadata path when present or missing Git metadata when absent, latest gstack health-history status/path, health-history `ts`/`timestamp` freshness against acceptance, latest local gstack browser QA report status/path and named screenshot paths, browser-QA freshness against acceptance, missing screenshot artifacts, and failed QA check rows instead of claiming unavailable workflow execution |
| Top-level objective audit can restate the prompt and map requirements to evidence | `scripts/goal-audit.ts`, `package.json` `audit:goal`, `.tmp/goal-audit/` | Implemented; writes JSON/Markdown prompt-to-artifact evidence for named files including `docs/OPERATOR_QUICKSTART.md`, the expanded package command surface for typecheck/test/build/preview/dev, plug-and-play setup/local-AI-prepare/doctor/rehearsal-start/smoke, source-control, browser QA, gstack health history, local AI, acceptance, bridge, bench, handoff, and audit commands, safety boundary, acceptance/release, API probe, source-control handoff readiness, completion audit, demo/handoff/bundle chain with bundle secret-scan coverage enforcement, gstack workflow-status artifact, local gstack QA report pointer when present, plug-and-play readiness, bounded rehearsal-start smoke, TODO/blocker consistency, demo-package perspective review, `docs/goal.md`, persisted remaining-blocker count/ID/list, and explicit real-world blockers. The audit derives blocker clearance, remaining-blocker list, persisted remaining-blocker IDs, and persisted remaining-blocker count from the computed completion audit, so partial future evidence can clear one hardware target without hiding the rest; it directly verifies API probe readback against the current acceptance timestamp, command count, exact required local-AI strict smoke scenario names, loopback Ollama URL, release checksum, and command-boundary scan, directly revalidates local AI prepare model/freshness against acceptance, requires source-control handoff evidence to be semantically valid, warning-free, clean, published to the expected GitHub repository, current against acceptance, and separate from hardware readiness, requires downstream demo/bench/handoff/bundle completion flags to match that audit, requires the review bundle and bundle verification to point at the latest gstack workflow-status, referenced QA report/screenshot artifacts, plug-and-play setup artifact, local AI prepare artifact, operator-start plug-and-play doctor artifact with source-control evidence matching the latest source-control handoff, rehearsal-start smoke artifact, strict local AI smoke status artifact, and packaged operator quickstart artifact, directly verifies the latest rehearsal-start smoke, fresh-clone smoke, handoff bundle, and bundle verification artifacts are not older than acceptance before trusting plug-and-play readiness, requires gstack workflow-status to include exact workflow/perspective row order, health-history metadata, perspective status/score/next-action details, a path when gstack health history or QA report status is `pass` or `stale`, Git metadata review evidence when present and no-Git review limitations when absent, requires the latest plug-and-play readiness audit to be newer than or equal to the latest acceptance record, reference the current API probe, setup, local AI prepare, operator-start doctor, source-control handoff, rehearsal-start smoke, review-bundle, workflow, TODO, and operator quickstart evidence, publish source-control repository URL/configured-remotes/local-branch/default-branch/ref-count/SHA/clean-worktree summaries plus review-bundle repository URL/configured-remotes/local-branch/default-branch/ref-count/SHA/clean-worktree and setup path/generatedAt/status summaries that match the latest handoff and bundle verification, and preserve the exact current completion-audit blocker ID/list in plug-and-play readiness, surfaces warning details from that plug-and-play readiness audit, requires its persisted blocker count to match the blocker list, requires TODO audit to match current evidence and blockers, and stays blocked today until validated physical evidence removes every real-world blocker |
| Completion and goal audits require the read-only bridge command surface | `scripts/completion-audit.ts`, `scripts/goal-audit.ts`, `package.json`, `src/server/__tests__/completionAudit.test.ts`, `src/server/__tests__/goalAudit.test.ts`, `src/server/__tests__/acceptanceScripts.test.ts` | Implemented; both audits now fail local alpha if `bridge:mavlink`, `bridge:mavlink:serial`, `bridge:ros2`, `bridge:ros2:live`, or `bridge:spatial` are missing from `package.json`, and the script-contract test pins those commands to their read-only entrypoints |
| README exposes the complete internal-alpha evidence command set | `README.md`, `src/server/__tests__/acceptanceScripts.test.ts`, `package.json` | Implemented; Useful Scripts includes the policy gate, completion audit, TODO audit, demo package, bench evidence packet, handoff index/verify/bundle/bundle-verify, gstack QA/health/workflow audit, and goal audit commands, with a regression test to keep the list aligned |
| Developer quickstart documents overnight evidence order accurately | `docs/DEVELOPER_QUICKSTART.md`, `scripts/overnight-loop.sh`, `src/server/__tests__/acceptanceScripts.test.ts` | Implemented; the quickstart now describes rehearsal-start smoke before release checksum plus the final API probe after `acceptance:record`, matching the watchdog script and session-visible acceptance evidence contract |
| Planning docs and test matrix reflect shipped local evidence commands | `docs/INTEGRATION_ROADMAP.md`, `docs/EDGE_HARDWARE_BENCH.md`, `docs/TEST_MATRIX.md`, `docs/SEEKR_GCS_ALPHA_TODO.md`, `src/server/__tests__/acceptanceScripts.test.ts` | Implemented; DimOS is documented as an existing deterministic read-only bench, flight-core/SITL status is no longer described as absent, the test matrix includes acceptance plus setup/local-AI/audit/demo/handoff evidence commands with command-block parsing that keeps `setup:local` before `ai:prepare` before `doctor` in the operator-start flow and reruns `setup:local`, `ai:prepare`, bounded rehearsal-start smoke, then final `doctor` before handoff bundling, and the GCS TODO keeps the real ROS 2 hardware blocker scoped to `/map`, pose, detection, LiDAR, and costmap topics |
| Edge and LiDAR research docs distinguish built wrappers from unproven hardware evidence | `docs/EDGE_HARDWARE_BENCH.md`, `docs/LIDAR_AI_DIMOS_RESEARCH.md`, `src/server/__tests__/acceptanceScripts.test.ts` | Implemented; the docs now describe MAVLink UDP/serial, ROS 2 live wrappers, source-health channels, and DimOS fixture bench as local read-only capabilities while keeping real-source MAVLink/ROS/LiDAR/DimOS proof in the hardware-blocker list |
| Session-visible acceptance evidence exists | `src/server/acceptanceEvidence.ts`, `scripts/acceptance-record.ts`, `/api/session` | Implemented; records strict AI, preview/build, checksum, required commands, current-boot state, command-boundary scan summary, and command boundary |
| Simulator/SITL flight core covers failsafes and rejects hardware actuation | `src/flight/*`, `src/flight/__tests__/*`, `scripts/flight-bench.ts`, `scripts/sitl-bench.ts`, `scripts/sitl-process-io.ts` | Implemented and bench-tested with fixtures |
| MAVLink bridge remains read-only | `src/server/adapters/mavlinkAdapter.ts`, `src/server/adapters/mavlinkBinary.ts`, `scripts/bridge-mavlink-readonly.ts`, `src/server/__tests__/adapters.test.ts`, `src/server/__tests__/readOnlyBridge.test.ts` | Fixture/file/stdin, binary capture parsing, and bounded UDP telemetry only; command methods reject |
| MAVLink serial bridge can be rehearsed without write streams | `scripts/bridge-mavlink-serial-readonly.ts`, `src/server/__tests__/mavlinkSerialBridge.test.ts`, `docs/EDGE_HARDWARE_BENCH.md` | Implemented as a bounded read-only serial byte capture wrapper over the existing MAVLink decoder; it never opens a serial write stream and posts only telemetry ingest |
| ROS 2 map/pose/detection/spatial bridge remains read-only | `src/server/adapters/ros2SlamAdapter.ts`, `fixtures/ros2-pose/*`, `scripts/bridge-ros2-readonly.ts`, `src/server/bridges/readOnlyBridge.ts`, `src/server/__tests__/readOnlyBridge.test.ts` | Fixture/file/stdin forwarding only; PoseStamped/Odometry maps to telemetry; topic-echo envelopes route pose, costmap, and PointCloud2 metadata to ingest; no services/actions exposed |
| Live ROS 2 topic bridge can be rehearsed without service/action calls | `scripts/bridge-ros2-live-readonly.ts`, `src/server/__tests__/ros2LiveBridge.test.ts`, `docs/EDGE_HARDWARE_BENCH.md` | Implemented as a bounded `ros2 topic echo --json` wrapper with command-preview mode; it streams only into read-only ingest endpoints and leaves real bench validation blocked until physical topics are captured |
| Bridge run evidence can be archived for real-source review | `scripts/bridge-evidence.ts`, `scripts/bridge-mavlink-readonly.ts`, `scripts/bridge-mavlink-serial-readonly.ts`, `scripts/bridge-ros2-live-readonly.ts`, `scripts/bridge-spatial-readonly.ts`, `.tmp/bridge-evidence/` | Implemented; MAVLink serial, bounded MAVLink UDP, live ROS 2, and spatial point-cloud wrappers write JSON/Markdown bridge evidence when `--evidence-label` or `--out-dir` is provided, preserving command-boundary flags without claiming hardware validation by itself |
| LiDAR/spatial/DimOS/Isaac-style imports are advisory read-only evidence | `fixtures/import/*.json`, `fixtures/spatial/*.json`, `scripts/dimos-readonly-bench.ts`, `docs/LIDAR_AI_DIMOS_RESEARCH.md` | Deterministic fixture benches exist; real Jetson capture still blocked |
| AI/Ollama stays advisory-only and server-constrained | `src/server/ai/*`, `scripts/ai-smoke.ts`, `src/server/__tests__/aiBoundaryLifecycle.test.ts`, `src/server/__tests__/aiTools.test.ts`, `src/server/ai/localAiEvidence.ts`, `src/server/__tests__/readiness.test.ts` | Implemented; strict local smoke uses a shared required scenario list for baseline assignment, no-fly drafting, and detection/spatial prompt-injection cases, and the evidence reader rejects missing scenarios or thinking-time mutation before acceptance can record local AI readiness |
| Optional internal token protects mutating routes without leaking secrets | `src/server/api/auth.ts`, `src/server/__tests__/apiContracts.test.ts`, `/api/session`, `/api/config` | Implemented and tested |
| Local-only browser/API exposure remains constrained | `src/server/__tests__/apiContracts.test.ts`, `docs/LOCAL_THREAT_MODEL.md` | Implemented; cross-origin requests do not receive CORS allow headers |
| Source health catches stale/missing expected sources | `src/server/sourceHealth.ts`, `src/server/__tests__/sourceHealth.test.ts`, `docs/EDGE_HARDWARE_BENCH.md` | Implemented and tested |
| Hardware readiness archives cannot be mistaken for actual board validation | `scripts/archive-hardware-probe.ts`, `src/server/__tests__/hardwareEvidenceArchive.test.ts`, `.tmp/hardware-evidence/` | Implemented; archives expose `actualHardwareValidationComplete`, per-target host validation, and `hardwareValidationScope`, reject empty target/report sets, invalid targets, mismatched target/report inputs, and invalid archive timestamps, and render target filename segments through the shared artifact filename helper |
| Acceptance/demo flows pass cleanly | `npm run acceptance`, `.tmp/overnight/STATUS.md` | Implemented; `npm run acceptance` and the bounded overnight loop pass on the current local-alpha artifact set |
| Long-running evidence stays under `.tmp` | `.tmp/overnight/`, `.tmp/hardware-evidence/`, docs retention policy | Implemented; deterministic fixtures remain in `fixtures/` |
| No hardware validation is claimed from fixtures/SITL | `docs/HARDWARE_DECISION_GATE.md`, `docs/SEEKR_GCS_ALPHA_TODO.md`, this file | Real-world blockers listed below |

## Current TODO

- Keep `npm run acceptance` as the single local alpha gate. It now runs check, edge bench, flight bench, SITL bench, SITL process IO fixtures, DimOS bench, command-boundary scan, strict local AI smoke, Playwright UI smoke, production preview smoke, bounded rehearsal-start smoke, release checksum, acceptance-status recording, and API probing.
- Run `npm run audit:completion` before readiness/demo reviews to capture the current local-alpha status and real-world blocker list under `.tmp/completion-audit/`.
- Run `npm run audit:todo` after `audit:completion` so unchecked TODOs in the GCS alpha TODO and completion plan stay machine-checked against the current real-world blocker categories.
- Run `npm run rehearsal:note -- --label <run-name>` before a field-laptop rehearsal and fill it after shutdown with evidence paths, replay id, final hash, timestamps, and deviations.
- Run `npm run rehearsal:closeout` only after a real field-laptop run; do not create a completed closeout with synthetic evidence.
- Keep `.tmp/overnight/STATUS.md` as the latest unattended verification record and archive logs only as evidence, not as source. Its remaining-TODO summary now includes both the GCS alpha TODO and completion-plan blocker lists.
- Capture a fresh-operator field-laptop rehearsal with setup, acceptance, `rehearsal:evidence` snapshots, export, replay, and shutdown notes.
- Run hardware readiness archive commands on an actual Jetson Orin Nano and Raspberry Pi 5.
- Connect read-only MAVLink and ROS 2 bridges to real bench telemetry/topics without exposing command services, and preserve `.tmp/bridge-evidence/` plus required-source rehearsal evidence from the same run.
- Capture HIL failsafe logs with manual override evidence.
- Capture Isaac Sim HIL fixture output from the Jetson bench.
- After the real HIL and target-board package exists, run `npm run policy:hardware:gate` to produce review evidence that still keeps all runtime authorization fields false.
- Run `npm run demo:package -- --label <demo-name>` after acceptance and completion audit to create internal handoff evidence that keeps hardware claims false and maps blocked real-world items to their next evidence commands.
- Run `npm run bench:evidence:packet -- --label <bench-name>` before a real bench session to turn the demo package into ordered operator task cards.
- Run `npm run handoff:index -- --label <handoff-name>` after the demo package and bench packet to verify the local evidence pointers are current before internal review.
- Run `npm run handoff:verify` after `handoff:index` to verify the recorded digest table before sending the handoff packet to a reviewer.
- Run `npm run qa:gstack` after acceptance or `npm run smoke:preview` to refresh the local browser QA report and desktop/mobile screenshots before final workflow packaging.
- Run `npm run health:gstack` after acceptance or the bounded overnight loop to refresh parser-compatible health-history JSONL before final workflow packaging.
- Run `npm run audit:gstack` after demo/handoff evidence, `qa:gstack`, and `health:gstack` are refreshed so the health, planning, review, QA workflow status, local gstack helper-tool evidence, and latest local gstack browser QA report/screenshot status are captured under `.tmp/gstack-workflow-status/`.
- Run `npm run audit:source-control` after the GitHub reference or local Git state changes to record package/README references, local Git metadata, configured remotes, local branch, remote refs/default branch, local HEAD publication, and clean-worktree status without publishing anything.
- Run `npm run setup:local` after install or after a new acceptance record to prepare `.env` and `.tmp/rehearsal-data` without overwriting operator edits or enabling command authority; final readiness and review-bundle checks require this setup artifact to be no older than acceptance.
- Run `npm run ai:prepare` after setup and before doctor/startup to prepare the local Ollama model and persist `.tmp/local-ai-prepare/` evidence without enabling command upload.
- Run `npm run smoke:fresh-clone` after the current HEAD is published to GitHub to prove a clean clone can install, run bounded operator-start smoke, run strict local AI smoke, rerun doctor, and keep command upload disabled.
- Run `npm run handoff:bundle -- --label <review-name>` after handoff verification, `health:gstack`, `audit:gstack`, `audit:todo`, `audit:source-control`, `setup:local`, `ai:prepare`, `smoke:rehearsal:start`, a final standalone `doctor` rerun, and `smoke:fresh-clone` to copy the verified local handoff artifacts plus the latest gstack workflow-status, referenced gstack QA report/screenshots, TODO audit, source-control handoff, plug-and-play setup, local AI prepare, strict local AI smoke status, operator-start plug-and-play doctor, rehearsal-start smoke, fresh-clone operator smoke, and operator quickstart artifacts into `.tmp/handoff-bundles/` without changing any hardware claim.
- Run `npm run handoff:bundle:verify` after bundling to recheck copied bundle file digests, copied gstack workflow semantics, copied gstack QA report/screenshot inclusion and report-content pass/command-safety, copied TODO audit exact category order and support semantics, copied source-control handoff semantics, copied plug-and-play setup semantics and freshness against copied acceptance, copied local AI prepare freshness and semantics, copied strict local AI smoke semantics, copied operator-start plug-and-play doctor semantics including source-control evidence that matches the copied source-control handoff, copied rehearsal-start smoke semantics, copied fresh-clone operator smoke semantics including strict-AI summary agreement, copied operator quickstart advisory-AI/no-command-payload/no-validation-bypass guidance, and full high-confidence secret-scan coverage before internal review.
- Run `npm run doctor` before local startup or final plug-and-play packaging to write an operator-start preflight that proves laptop startup prerequisites, repository safety via `.gitignore`/`.npmrc`, `setup:local` script presence, `plug-and-play` alias delegation, `rehearsal:start` wrapper safety, local Ollama availability, default port readiness or read-only listener diagnostics, data directory safety, and disabled command-upload/hardware-actuation flags. The plug-and-play wrapper normalizes `PORT`/`SEEKR_API_PORT` and auto-selects free local ports when unconfigured defaults are busy; the doctor records current fallback port candidates so the review packet shows a concrete alternate URL path. Rerun it after bounded rehearsal-start smoke before bundling so the operator-start doctor references the latest source-control handoff artifact.
- Run `npm run plug-and-play` to launch the local plug-and-play rehearsal wrapper with project-local data, expected read-only source defaults, API/client port normalization, fast failure for conflicting explicit `PORT`/`SEEKR_API_PORT` values, automatic free-port fallback for unconfigured default conflicts, automatic non-destructive setup, automatic local AI preparation, source-control refresh, and an automatic doctor preflight before `npm run dev`.
- Run `npm run smoke:rehearsal:start` to prove the same local operator wrapper can start on temporary ports, generate setup/local-AI/source-control/smoke-doctor evidence, expose API/client/source-health/readiness, keep command upload disabled, and shut down cleanly while marking its internal doctor artifact as `profile: "rehearsal-start-smoke"` so it cannot replace the operator-start doctor in review packets.
- Run `npm run audit:plug-and-play` after `audit:source-control`, `setup:local`, `ai:prepare`, `smoke:rehearsal:start`, the final standalone `doctor`, `smoke:fresh-clone`, and bundle verification to prove local app, advisory-only AI, API readback matched to acceptance, QA, review-bundle, source-control handoff, setup freshness plus copied setup summary agreement, local AI prepare, strict local AI smoke, operator-start doctor, rehearsal-start smoke, fresh-clone operator smoke, operator quickstart command-safety guidance, rehearsal-start, and safety-boundary readiness before final objective review; explicit occupied-port warnings remain visible, while unconfigured default-port conflicts can pass when the rehearsal-start free-port fallback is verified.
- Run `npm run audit:goal` after handoff verification, `health:gstack`, `audit:gstack`, `audit:todo`, `audit:source-control`, `setup:local`, `ai:prepare`, `smoke:rehearsal:start`, the final standalone `doctor`, bundling, bundle verification, and `audit:plug-and-play` when preparing the final internal-alpha review packet; it restates the objective as a prompt-to-artifact checklist, directly checks setup freshness against acceptance, and keeps real-world blockers explicit.
- Keep the GStack Workflow Status section and `.tmp/gstack-workflow-status/` artifact current after health, planning, review, and QA passes. If the workspace still lacks Git metadata, record diff-review as unavailable instead of pretending a pre-landing review ran.
- Do not add a hardware-actuation policy file until a specific bench vehicle, manual override, E-stop, and review package exist.

## Acceptance Expectations

Before calling the alpha complete for a demo/audit run:

1. `npm run check` passes.
2. `npm run bench:edge` passes without touching command endpoints.
3. `npm run bench:flight` passes and proves simulator/SITL command sequencing plus hardware rejection.
4. `npm run bench:sitl` and both `npm run bench:sitl:io` fixture runs pass with `commandUploadEnabled: false`.
5. `npm run bench:dimos` passes as a read-only export/import contract.
6. `npm run safety:command-boundary` passes and writes `.tmp/safety-evidence/` static scan evidence.
7. `npm run test:ai:local` passes with local Ollama required, or the run is explicitly labeled non-acceptance deterministic-rules testing.
8. `npm run test:ui` passes across the focused operator workflows.
9. `npm run smoke:preview` passes, proving the built shell and API serve together.
10. `npm run release:checksum` writes field-laptop install-integrity evidence under `.tmp/release-evidence/`.
11. `npm run acceptance:record` writes `.tmp/acceptance-status.json` with the latest passing command-boundary scan summary, rejects acceptance output/status/release/safety evidence paths that escape the project root, and `/api/session` reports the latest acceptance status without exposing secrets.
12. `npm run probe:api` verifies the final session-visible acceptance evidence, including strict local AI scenario names and loopback Ollama URL, readiness, hardware readiness, source health, replay listing, hash verification, and malformed JSON handling, then writes `.tmp/api-probe/` evidence.
13. `npm run audit:completion` writes the current local-alpha status and real-world blockers after verifying acceptance matches the latest release checksum, command-boundary scan, and API-probe evidence; Jetson Orin Nano and Raspberry Pi 5 hardware validation blockers are tracked separately.
14. `npm run demo:package -- --label <demo-name>` writes the internal demo/audit handoff manifest.
15. `npm run bench:evidence:packet -- --label <bench-name>` writes the real-world evidence collection task cards when a bench run is being planned.
16. `npm run handoff:index -- --label <handoff-name>` writes a final pointer-consistency and digest index for the latest local handoff evidence, including acceptance-to-safety-scan consistency and API-probe acceptance timestamp/command-count/exact strict-AI readback with loopback Ollama URL.
17. `npm run handoff:verify` writes a digest verification report for the latest or specified handoff index.
18. `npm run qa:gstack` regenerates the local gstack browser QA report and desktop/mobile screenshots from a clean production-shell run.
19. `npm run health:gstack` runs `npm run check` and appends parser-compatible gstack health-history JSONL with command upload disabled.
20. `npm run audit:gstack` writes `.tmp/gstack-workflow-status/` with health, planning, review, QA workflow status, local gstack helper-tool root/count/name evidence when present, latest gstack health-history status/path, latest local gstack browser QA report status/path and screenshot paths when present, operator/safety/DX/replay/demo-readiness perspectives, and workspace limitations.
21. `npm run audit:source-control` writes `.tmp/source-control-handoff/` with package/README GitHub references, local Git metadata, configured remotes, local branch, GitHub remote refs/default branch, local HEAD versus the GitHub default branch SHA, clean-worktree status, exact ordered source-control check IDs, persisted blocked/warning check counts, and manual next-action steps for missing `.git`, origin, refs, unpublished HEAD, dirty worktrees, and re-audit, without committing or pushing.
22. `npm run audit:todo` writes `.tmp/todo-audit/` and verifies unchecked planning TODOs still cover the current completion-audit real-world blocker categories.
23. `npm run setup:local` writes `.tmp/plug-and-play-setup/` and prepares `.env` plus `.tmp/rehearsal-data` without overwriting an existing `.env`; final readiness and review bundles require the artifact to be no older than acceptance.
24. `npm run ai:prepare` writes `.tmp/local-ai-prepare/` after a passing local Ollama model preparation command, defaulting to `ollama pull llama3.2`.
25. `npm run doctor` writes `.tmp/plug-and-play-doctor/` and proves local startup prerequisites for package scripts, package `engines`/`packageManager`, Node/package-lock readiness, `.npmrc` `engine-strict`, `.gitignore` protection for local secrets/generated artifacts/runtime `data/`, local `tsx`/`concurrently`/`vite` dependency binaries needed by `npm run dev`, the `plug-and-play` alias, the `setup:local` and `ai:prepare` commands, the `rehearsal:start` wrapper contract including API/client port normalization and free-port fallback for unconfigured default conflicts, env setup, local Ollama model availability, API/client port availability, healthy already-running SEEKR listeners, auto-recoverable unconfigured default-port conflicts, read-only PID/cwd diagnostics for occupied non-SEEKR ports, local data directory safety, and disabled command-upload/hardware-actuation flags.
26. `npm run plug-and-play` starts the local plug-and-play rehearsal wrapper with project-local `.tmp/rehearsal-data`, expected read-only source categories, normalized `PORT`/`SEEKR_API_PORT`, fast failure for conflicting explicit API port variables, free local API/client ports when unconfigured defaults are busy, non-destructive `npm run setup:local`, `npm run ai:prepare`, fresh `npm run audit:source-control`, `npm run doctor`, and `npm run dev`.
27. `npm run smoke:rehearsal:start` writes `.tmp/rehearsal-start-smoke/` after launching `npm run rehearsal:start` on temporary local ports, confirming wrapper-generated setup/local-AI/source-control/smoke-doctor artifacts, checking API/client/source-health/readiness, and shutting down cleanly.
28. `npm run doctor` is rerun after smoke so the final operator-start doctor references the latest source-control handoff refreshed by the wrapper, not the smoke-profile doctor.
29. `npm run handoff:bundle -- --label <review-name>` writes a copied local review bundle from the verified handoff index, linked artifacts, latest gstack workflow-status artifact, referenced local gstack QA report/screenshots, latest TODO audit artifact, latest source-control handoff artifact, latest plug-and-play setup artifact that is fresh against acceptance, latest local AI prepare artifact, latest operator-start plug-and-play doctor artifact, latest rehearsal-start smoke artifact, and operator quickstart.
30. `npm run handoff:bundle:verify` writes a copied-bundle digest, copied acceptance/API-probe strict local AI readback check including loopback Ollama URL, copied gstack workflow semantic check, copied gstack QA report/screenshot inclusion and report-content check, copied TODO audit exact-category-order semantic check, copied source-control handoff semantic check, copied plug-and-play setup semantic/freshness check, copied local AI prepare semantic check, copied operator-start plug-and-play doctor semantic check including source-control evidence matching, copied rehearsal-start smoke semantic check, copied operator quickstart advisory-AI/no-command-payload/no-validation-bypass semantic check, and high-confidence secret-scan coverage report for the latest or specified handoff bundle.
31. `npm run audit:plug-and-play` writes `.tmp/plug-and-play-readiness/` and proves local plug-and-play readiness for app, advisory-only local AI with loopback Ollama URL, API readback matched to acceptance, QA, source-control handoff, review bundle, setup freshness plus copied setup summary agreement, local AI prepare, operator-start doctor, rehearsal-start smoke, operator quickstart command-safety guidance, rehearsal-start, and safety boundary.
32. `npm run audit:goal` writes the prompt-to-artifact objective audit, directly verifies setup freshness against acceptance, and must report local alpha ready but real-world blocked until physical evidence exists.
33. `npm run status:local` writes `.tmp/recovery-status/` as a lightweight post-interruption readback across the latest local artifacts without rerunning acceptance or clearing real-world blockers.
31. `.tmp/overnight/STATUS.md` is checked and any failures are reconciled before demo.
31. Any hardware-facing statement names the evidence source exactly: fixture, SITL, HIL, Jetson bench, Pi bench, or real read-only adapter.

## GStack Workflow Status

Health: current local health is clean for typecheck, Vitest, and Playwright UI QA; shell script linting is skipped because `shellcheck` is not installed in this environment. The local health history file is under `~/.gstack/projects/software/health-history.jsonl`; `npm run health:gstack` refreshes it through the package-script path, and `npm run audit:gstack` records it as stale when the latest entry predates the current acceptance record or lacks a parseable `ts` or `timestamp` field.

Planning: `docs/goal.md`, `docs/SEEKR_COMPLETION_PLAN.md`, `docs/SEEKR_GCS_ALPHA_TODO.md`, `npm run audit:completion`, `npm run audit:todo`, `npm run audit:gstack`, and `npm run audit:goal` are the active plan-review chain. The current plan keeps the operator, safety, DX, replay, and demo-readiness perspectives explicit while refusing to clear real-world blockers with fixture/SITL evidence.

Review: source-control handoff is now ready for the GitHub repository and Git metadata is available from the software workspace at `../.git`. The workflow artifact records that path for base-branch diff review, while still not claiming an independent pre-landing review unless a future review artifact is produced; safety review is still covered by `npm run safety:command-boundary`, `npm run handoff:verify`, and `npm run audit:goal`.

QA: Playwright exercises the field-operator shell, mission controls, no-fly planning, evidence review, spatial preview, artifacts, readiness, source health, replay, API error modals, and field-laptop viewports. No critical/high/medium QA issues were found in the current pass.

Workflow artifact: `npm run audit:gstack` records the current gstack health/review/planning/QA mapping under `.tmp/gstack-workflow-status/`, including per-workflow installed skill availability, whether the `gstack` CLI is available on PATH, local gstack helper-tool root/count/name evidence when helper tools are installed without the umbrella CLI, Git metadata path or no-Git limitation status, health-history path/freshness, demo package perspective status/score/next-action critiques, and latest local gstack browser QA report status/path plus screenshot paths.

## Latest Verification

Current-turn verification on 2026-05-12:

| Command | Result | Evidence |
| --- | --- | --- |
| `npm run test -- goalAudit acceptanceScripts` | Pass | 2 test files, 61 tests passed, covering the expanded goal-audit named-command surface for typecheck/test/build/preview/dev, plug-and-play setup/doctor/rehearsal-start/smoke, source-control, browser QA, local AI, acceptance, bridge, bench, handoff, and audit commands, plus direct source-control handoff blocking when the artifact is unpublished or dirty and direct local AI prepare freshness, semantic, and model-match blocking |
| `npm run test -- gstackWorkflowStatus acceptanceScripts` | Pass | 2 test files, 18 tests passed, covering gstack helper-tool root/count/name evidence when helper tools are installed without the umbrella CLI, workflow manifest generation, and docs/package command guards |
| `npm run test -- plugAndPlayDoctor plugAndPlayReadiness handoffBundle acceptanceScripts goalAudit` | Pass | 5 test files, 159 tests passed, covering package `engines`/`packageManager` doctor enforcement, repository-safety doctor enforcement including runtime `data/` ignore coverage, runtime dependency evidence preservation, plug-and-play readiness including advisory-AI operator quickstart coverage, handoff bundle doctor and operator-quickstart semantic verification, package-script coverage, and goal-audit enforcement |
| `npm run test -- handoffBundle goalAudit todoAudit acceptanceScripts` | Pass | 4 test files, 130 tests passed, covering TODO audit generation, partial blocker-category clearance, missing/stale TODO audit blocking in goal audit, exact ordered TODO audit category preservation in review bundles, copied TODO audit semantic verification including top-level summary counts and well-formed category support evidence, copied setup, doctor, and advisory-AI operator quickstart semantic verification including repository-safety and runtime dependency evidence preservation, package-script coverage, README command coverage, and test-matrix coverage |
| `npm run test -- gstackWorkflowStatus handoffBundle goalAudit todoAudit acceptanceScripts` | Pass | 5 test files, 111 tests passed, covering machine-readable gstack workflow status, stale browser-QA freshness recording, failed browser-QA check row blocking in workflow and copied-bundle verification, missing browser-QA screenshot blocking, unparseable health-history timestamp handling, required health-history metadata/path, required QA report metadata/path/screenshot paths when QA is marked pass or stale, local gstack QA report detection, CLI availability recording, parent Git metadata path detection, required per-workflow installed skill availability, top-level `pass-with-limitations` status for limitation-only evidence, manifest-level limitation preservation including unavailable gstack CLI details, stale/missing health and QA limitation-detail preservation, perspective status/score/next-action preservation, Git review evidence or no-Git review limitations in workflow artifacts and review bundles, required TODO blocker-category/top-level blocker-count/well-formed support-evidence preservation, copied setup, doctor, and advisory-AI operator quickstart semantic verification in review bundles including repository-safety and runtime dependency evidence preservation, critical-versus-soft doctor check status semantics, handoff-bundle semantic checks, TODO/blocker consistency, package-script coverage, README command coverage, and test-matrix coverage |
| `npm run test -- gstackBrowserQa acceptanceScripts gstackWorkflowStatus handoffBundle goalAudit todoAudit` | Pass | 6 test files, 111 tests passed, covering repeatable browser-QA report rendering, generatedAt ISO/path-safe timestamp enforcement, package-script/docs coverage for `qa:gstack`, gstack workflow status, handoff bundle QA screenshot plus copied setup, doctor, and advisory-AI operator quickstart preservation with critical-versus-soft doctor check status semantics, TODO/blocker consistency, and goal-audit real-world blocker preservation |
| `npm run test -- hardwareEvidenceArchive artifactPaths gstackBrowserQa acceptanceScripts gstackWorkflowStatus handoffBundle goalAudit todoAudit` | Pass | 8 test files, 123 tests passed, covering hardware archive fail-closed target/report/timestamp input validation, shared normalized artifact timestamp filename sanitization, shared filename-label sanitization, output-directory root containment, source-contract enforcement that evidence writers use the shared output guard and avoid local sanitizer drift, repeatable browser-QA report rendering, package-script/docs coverage for `qa:gstack`, gstack workflow status, handoff bundle QA screenshot plus copied setup, doctor, and advisory-AI operator quickstart preservation with critical-versus-soft doctor check status semantics, TODO/blocker consistency, and goal-audit real-world blocker preservation |
| `npm run test -- artifactPaths hilFailsafeEvidence isaacHilCaptureEvidence hardwareActuationPolicyGate completionAudit acceptanceScripts` | Pass | 6 test files, 44 tests passed, covering referenced evidence/log/policy path root containment for HIL, Isaac, and policy-gate artifacts plus completion-audit and docs/package command guards |
| `npm run test -- acceptanceScripts artifactPaths` | Pass | 2 test files, 14 tests passed, covering acceptance recorder CLI path containment, package/docs command guards, and shared artifact path helpers |
| `npm run test -- localRecoveryStatus plugAndPlayReadiness acceptanceScripts` | Pass | 3 test files, 82 tests passed, covering interrupted-run local recovery status generation, stale acceptance release JSON/SHA/Markdown, command-boundary JSON/Markdown/count evidence, and stale `/api/session` acceptance readback blocking, strict local AI case-name drift blocking, fresh-clone strict AI proof blocking, stale source-control HEAD blocking for fresh-clone, plug-and-play, and review-bundle evidence, plug-and-play command-surface coverage for `status:local`, docs/package command guards, and test-matrix command coverage |
| `npm run test -- goldenScenarios` | Pass | 1 test file, 2 tests passed after giving the byte-stable scenario replay regression an explicit timeout for loaded local health runs |
| `npm run audit:gstack` | Pass with workspace limitation | Wrote latest `.tmp/gstack-workflow-status/seekr-gstack-workflow-status-*.{json,md}` with 4 workflows, 5 perspectives, `healthHistoryStatus`, local gstack QA report status/freshness, `gstackAvailable: true`, explicit `gstackCliAvailable`, `gitMetadataPath: "../.git"`, `commandUploadEnabled: false`, and review workflow evidence pointing at parent Git metadata |
| `npm run audit:goal` | Pass, real-world blocked | Wrote latest `.tmp/goal-audit/seekr-goal-audit-*.{json,md}` after requiring the current gstack workflow-status artifact; status remains `local-alpha-ready-real-world-blocked`, `complete: false`, and `commandUploadEnabled: false` |
| `npm run test -- rehearsalCloseout rehearsalNote completionAudit acceptanceScripts` | Pass | 4 test files, 11 tests passed |
| `npm run test -- readOnlyBridge` | Pass | 1 test file, 8 tests passed, including MAVLink binary capture parsing, UDP listener ingest, checksum rejection, and ROS 2 topic-echo envelope routing |
| `npm run test -- readOnlyBridge adapters sourceHealth` | Pass | 3 test files, 16 tests passed, including ROS 2 PoseStamped/Odometry telemetry mapping and topic-echo routing |
| `npm run bridge:ros2 -- --dry-run --stdin --topic /drone-cli/pose --received-at 1800000005000` | Pass | CLI stdin smoke accepted one PoseStamped-style record, posted 0 events, and reported `commandEndpointsTouched: false` |
| `npm run bridge:mavlink -- --dry-run --stdin --received-at 1800000005000` | Pass | CLI stdin smoke accepted one HEARTBEAT-style record, posted 0 events, and reported `commandEndpointsTouched: false` |
| `npm run bridge:spatial -- --dry-run --stdin --received-at 1800000005000` | Pass | CLI stdin smoke accepted one point-cloud spatial asset, posted 0 events, and reported `commandEndpointsTouched: false` |
| `npm run test -- readOnlyBridge acceptanceScripts` | Pass | 2 test files, 18 tests passed, including persisted spatial `.tmp/bridge-evidence/` artifacts and bridge/doc command coverage |
| `npm run bridge:spatial -- --dry-run --fixture lidar-point-cloud --evidence-label spatial-preview-smoke --generated-at 2026-05-09T22:50:00.000Z` | Pass dry-run evidence | Wrote `.tmp/bridge-evidence/seekr-bridge-evidence-spatial-assets-spatial-preview-smoke-2026-05-09T22-50-00-000Z.{json,md}` with `commandEndpointsTouched: false`, `commandUploadEnabled: false`, and the expected dry-run warning |
| `npm run test -- completionAudit goalAudit acceptanceScripts` | Pass | 3 test files, 24 tests passed, including completion/goal audit coverage for required read-only bridge package scripts and exact package-script guards for the read-only bridge entrypoints |
| `npm run bridge:mavlink -- --dry-run --hex <heartbeat-frame> --received-at 1800000003000` | Pass | CLI decoded one MAVLink v2 heartbeat frame, posted 0 events, and reported `commandEndpointsTouched: false` |
| `npm run test -- hilFailsafeEvidence completionAudit acceptanceScripts` | Pass | 3 test files, 11 tests passed, including completed/blocked HIL evidence cases and completion-audit HIL recognition |
| `npm run test -- isaacHilCaptureEvidence completionAudit acceptanceScripts` | Pass | 3 test files, 12 tests passed, including completed/blocked Isaac capture evidence cases and completion-audit Isaac recognition |
| `npm run test -- hardwareActuationPolicyGate completionAudit acceptanceScripts` | Pass | 3 test files, 14 tests passed, including fail-closed policy gate evidence, unsafe policy rejection, completion-audit recognition, and required script coverage |
| `npm run test -- demoReadinessPackage acceptanceScripts completionAudit` | Pass | 3 test files, 34 tests passed, including local-alpha package generation, stale overnight warning, acceptance/release mismatch blocking, acceptance/safety-scan mismatch blocking, completion-audit blocking, source-control DX freshness against acceptance, and required script coverage |
| `npm run test -- demoReadinessPackage benchEvidencePacket acceptanceScripts` | Pass | 3 test files, 21 tests passed, including MAVLink serial and live ROS 2 bridge commands in next-evidence guidance, source-control DX freshness, and bench task cards |
| `npm run test -- apiProbe completionAudit demoReadinessPackage handoffIndex handoffVerification acceptanceScripts` | Pass | 6 test files, 46 tests passed, including persisted API-probe evidence, completion-audit API-probe matching, demo-package API-probe and source-control freshness pointers, handoff-index API-probe digesting, handoff verification, and acceptance script coverage |
| `npm run test -- goalAudit` | Pass | 1 test file, 39 tests passed, including prompt-to-artifact objective audit generation, missing command detection, required handoff verification detection, direct API readback-to-acceptance and exact AI-summary/loopback-Ollama-URL matching, untracked extra strict-AI scenario blocking, strict local AI smoke status pointer enforcement, plug-and-play acceptance/API freshness guards, and plug-and-play warning surfacing |
| `npm run test -- benchEvidencePacket demoReadinessPackage completionAudit acceptanceScripts` | Pass | 4 test files, 39 tests passed, including ordered bench task cards, unsafe demo package blocking, source-control DX freshness, next-evidence checklist packaging, completion-audit coverage, and required script coverage |
| `npm run test -- commandBoundaryScan acceptanceScripts completionAudit` | Pass | 3 test files, 13 tests passed, including static scan pass/fail cases, unsafe command pattern detection, acceptance script coverage, and completion-audit scan recognition |
| `npm run test -- handoffIndex acceptanceScripts completionAudit` | Pass | 3 test files, 17 tests passed, including handoff index pointer validation, linked-artifact digest capture, acceptance-to-safety-scan consistency, stale demo/audit blocking, unsafe bench packet blocking, required script coverage, and completion-audit script coverage |
| `npm run test -- handoffVerification handoffIndex acceptanceScripts completionAudit` | Pass | 4 test files, 18 tests passed, covering handoff digest verification, tamper detection, safety-boundary checks, handoff index behavior, required script coverage, and completion-audit script coverage |
| `npm run test -- releaseChecksums acceptanceScripts demoReadinessPackage handoffIndex handoffVerification completionAudit` | Pass | 6 test files, 47 tests passed, covering release checksum evidence including repository-safety files, package/runtime version alignment, acceptance command recording including command-boundary scan, demo release/safety/source-control freshness matching, handoff index acceptance/demo-to-safety-scan consistency, handoff verification, completion-audit acceptance-to-release/safety consistency, and completion-audit script coverage |
| `npm run test -- acceptanceScripts` | Pass | 1 test file, 7 tests passed, including README Useful Scripts, developer-quickstart and overnight-loop evidence order, roadmap DimOS status, edge-bench flight-core status, edge-bench bridge/DimOS wording guards, and test-matrix command guards |
| `npm run test -- acceptanceScripts` | Pass | 1 test file, 7 tests passed after aligning the edge/LiDAR research docs so shipped read-only wrappers are not described as still unbuilt, while preserving the overnight-loop rehearsal-start smoke guard |
| `npm run test -- startupLog` | Pass | 1 test file, 1 test passed, covering bounded startup hash-chain validation diagnostics |
| `npm run test -- ros2LiveBridge acceptanceScripts` | Pass | 2 test files, 9 tests passed, covering live ROS 2 topic command preview, topic validation, package script exposure, README command coverage, and test-matrix coverage |
| `npm run test -- mavlinkSerialBridge ros2LiveBridge demoReadinessPackage benchEvidencePacket acceptanceScripts` | Pass | 5 test files, 29 tests passed, covering persisted `.tmp/bridge-evidence/` artifacts for MAVLink serial and live ROS 2 command previews/runs plus updated demo source-control freshness and bench guidance |
| `npm run test -- completionAudit demoReadinessPackage benchEvidencePacket acceptanceScripts` | Pass | 4 test files, 39 tests passed, including the stricter completion-audit rules that real MAVLink/ROS blockers require actual target-board archive scope, HIL/Isaac/policy blockers require valid referenced evidence files, and demo DX review rejects stale ready source-control handoff evidence |
| `npm run test -- readOnlyBridge completionAudit benchEvidencePacket acceptanceScripts` | Pass | 4 test files, 34 tests passed, including bounded MAVLink UDP `.tmp/bridge-evidence/` artifacts, completion-audit recognition of live UDP listener evidence, and bench task card criteria for serial or UDP evidence |
| `npm run bridge:ros2:live -- --command-preview --dry-run --topic /drone/pose,/map,/lidar/points --evidence-label ros2-preview-smoke` | Pass command preview | Prints three `ros2 topic echo --json` subscriptions, does not spawn ROS 2, writes `.tmp/bridge-evidence/`, reports `commandEndpointsTouched: false`, no ROS services/actions touched, and `commandUploadEnabled: false` |
| `npm run test -- acceptanceScripts` | Pass | 1 test file, 7 tests passed, including the GCS TODO guard that keeps the real ROS 2 blocker tied to `/map`, pose, detection, LiDAR, and costmap bench topics plus the overnight-loop rehearsal-start smoke order guard |
| `npm run test -- acceptanceScripts` | Pass | 1 test file, 7 tests passed, including docs guards that keep README, developer quickstart, dedicated overnight-loop docs, and V1 acceptance aligned with the demo package perspective-review contract |
| `npm run typecheck` | Pass | TypeScript completed with no errors during gstack health verification |
| `npm run test` | Pass | 58 test files, 575 tests passed during the current local verification pass |
| `npm run test:ui` | Pass | 6 Playwright tests passed, covering operator-gated mission controls, evidence/spatial views, artifacts/readiness/source-health/replay, API error modal handling, and field-laptop viewport fit |
| `npm run test -- goalAudit` | Pass | 1 test file, 39 tests passed, including the `gstack-workflow-status` prompt-to-artifact checklist guard, complete-only-when-completion-audit-complete regression, stale handoff-chain blocking regression, direct API readback-to-acceptance and exact AI-summary/loopback-Ollama-URL matching, untracked extra strict-AI scenario blocking, strict local AI smoke status pointer enforcement, plug-and-play acceptance/API freshness guards, and plug-and-play warning surfacing |
| `npm run test -- demoReadinessPackage acceptanceScripts` | Pass | 2 test files, 16 tests passed, covering source-control-aware demo DX perspective review, source-control freshness against acceptance, and docs/package command guards |
| `npm run test -- demoReadinessPackage benchEvidencePacket goalAudit acceptanceScripts` | Pass | 4 test files, 90 tests passed, including demo package perspective review, source-control DX freshness, bench packet compatibility with the new demo manifest field, and goal-audit enforcement that the latest demo package includes operator, safety, DX, replay, and demo-readiness critiques |
| `npm run audit:goal` | Pass, real-world blocked | Wrote latest `.tmp/goal-audit/seekr-goal-audit-*.{json,md}` evidence with 9 pass, 1 blocked, `complete: false`, and `commandUploadEnabled: false` |
| `npm run test -- mavlinkSerialBridge acceptanceScripts` | Pass | 2 test files, 9 tests passed, covering read-only MAVLink serial command preview, argument parsing, bounded serial-byte capture dry-run, package script exposure, README command coverage, and test-matrix coverage |
| `npm run bridge:mavlink:serial -- --command-preview --dry-run --device /dev/ttyUSB0 --evidence-label mavlink-preview-smoke` | Pass command preview | Prints read-only serial capture settings without opening the device, writes `.tmp/bridge-evidence/`, reports `serialWriteOpened: false`, `commandEndpointsTouched: false`, and `commandUploadEnabled: false` |
| `npm run safety:command-boundary` | Pass | Writes `.tmp/safety-evidence/seekr-command-boundary-scan-*.{json,md}` with `status: pass`, 132 scanned files, 0 violations, and `commandUploadEnabled: false` |
| `npm run policy:hardware:gate -- --label current-missing-evidence --command-upload-enabled false` | Expected blocked | Wrote `.tmp/policy-evidence/seekr-hardware-actuation-gate-current-missing-evidence-*.{json,md}` with `commandUploadEnabled: false`, false authorization fields, and blockers for missing real policy/evidence package |
| `npm run test -- rehearsalEvidence completionAudit acceptanceScripts` | Pass | 3 test files, 11 tests passed, including required-source rehearsal evidence pass/fail cases and evidence-aware completion-audit bridge clearing |
| `npm run test -- rehearsalEvidence acceptanceScripts` | Pass | 2 test files, 3 tests passed |
| `npm run test -- rehearsalNote completionAudit acceptanceScripts` | Pass | 3 test files, 7 tests passed |
| `npm run test -- completionAudit acceptanceScripts` | Pass | 2 test files, 13 tests passed, including completion-audit acceptance-to-release/safety consistency and required script coverage |
| `npm run test -- hardwareEvidenceArchive artifactPaths acceptanceScripts` | Pass | 3 test files, 17 tests passed, including hardware archive fail-closed input validation, target filename segment preservation, artifact path helper coverage, and docs/package command guards |
| `npm run test -- apiContracts acceptanceEvidence acceptanceScripts` | Pass | 3 test files, 15 tests passed, including `/api/session` release checksum and command-boundary scan summaries for passing acceptance evidence and hermetic missing-acceptance contract fixtures |
| `npm run typecheck` | Pass | `tsc --noEmit` completed successfully |
| `npm run check` | Pass | 58 test files, 575 tests passed |
| gstack health workflow | Pass with skipped categories | `npm run typecheck` pass; `npm run test` pass with 58 test files / 575 tests after source-control-aware demo DX perspective review, plug-and-play recovery status coverage, source-control handoff audit command/artifact coverage, source-control handoff warning-state blocking, shared source-control handoff semantic validation, exact ordered source-control check-ID validation, local branch preservation, local HEAD publication and clean-worktree gates, ordered GitHub landing README fresh-clone validation, downstream rejection of landing README pass artifacts missing ordered-command evidence, GitHub landing README fenced-shell command-line proof ordering plus non-negated explicit disabled command-upload/hardware-actuation wording and contradictory enabled/allowed authority rejection that cannot be satisfied by prose-only command mentions, shell comments, echoed strings, or `not disabled`/`not locked` statements or contradictory enabled/allowed authority statements, shallow fresh-clone startup-file, cloned landing README contract, shared operator-quickstart contract, published fresh-clone operator smoke validation with `npm ci`, operator quickstart non-negated command/hardware boundary validation plus contradictory enabled/allowed authority rejection, and `npm ci --dry-run` smoke validation, persisted source-control check-count validation, persisted source-control publication next-action validation, warning-bearing source-control remote-ref artifacts remaining soft doctor warnings but blocking plug-and-play readiness, unsafe source-control artifact blocking in doctor/readiness/bundle verification, source-control handoff review-bundle packaging/verification with top-level repository URL/configured-remotes/local-branch/default-branch/ref-count/blocked-count/warning-count/local/remote SHA and clean-worktree summary agreement, source-control repository URL/configured-remotes/local-branch/default-branch/ref-count summary drift blocking in plug-and-play readiness and goal audit, source-control handoff generatedAt freshness against acceptance in demo DX review, bundle creation, bundle verification, plug-and-play readiness, and direct top-level goal audit, health-history `timestamp` compatibility, package-scripted parser-compatible health-history recording, hermetic missing-acceptance API contract fixtures, local gstack helper-tool root/count/name evidence plus unavailable-CLI helper-tool preservation in bundle, copied-bundle, goal-audit, and plug-and-play readiness paths, release checksum coverage for repository-safety files, handoff-index release checksum path/metadata freshness, local recovery release/safety auxiliary path and command-boundary count hardening, local recovery API-readback freshness hardening, local setup command/env/data preparation coverage, first-class local AI model-prep artifact coverage, local AI prepare model-to-acceptance binding, generatedAt freshness against acceptance, exact `ollama pull <model>` command-shape/evidence pinning with safe model-argument, shell-metacharacter argument rejection, relative lookalike executable rejection, strict AI command-boundary evidence preservation, and direct goal-audit/plug-and-play readiness rejection of unsafe or wrong-model local AI prepare artifacts, shared plug-and-play setup/operator-start doctor artifact contract coverage, operator doctor source-control evidence matching against the packaged source-control handoff, setup artifact packaging/verification and stale setup review-bundle blocking, local plug-and-play doctor package `engines`/`packageManager`, repository-safety `.gitignore`/`.npmrc`/runtime `data` enforcement, runtime-dependency dev-server-binary, healthy already-running SEEKR port recognition, auto-recoverable unconfigured default-port conflict recognition, explicit occupied-port warning coverage, read-only per-port listener diagnostics and copied PID evidence for occupied non-SEEKR ports, shared doctor contract rejection of non-SEEKR port warnings without listener diagnostics and auto-recoverable port passes without per-occupied-port fallback candidate proof, exact printed non-default fallback-port proof, or `npm run plug-and-play` operator guidance, and plug-and-play readiness contract coverage for that recognition, start-wrapper API-port normalization and free-port fallback coverage, bounded rehearsal-start smoke exact-row and prerequisite artifact-pointer coverage, post-acceptance freshness blocking for rehearsal-start smoke, fresh-clone smoke, handoff bundle, bundle verification, and top-level goal-audit evidence, copied local AI prepare model-prep semantics, copied doctor repository-safety/source-control-handoff/runtime dependency evidence/start-wrapper verification and critical-versus-soft doctor check status semantics in review bundles, operator-start versus rehearsal-start-smoke doctor profile separation, shared operator-quickstart contract coverage including install-before-setup/start command ordering and the bounded rehearsal-start smoke proof command/order, plug-and-play readiness setup/local AI prepare/doctor/artifact row/status/evidence enforcement plus exact-row operator-start doctor, current gstack workflow/perspective rows, exact-row rehearsal-start smoke, and operator quickstart GitHub clone/software-directory setup/ai:prepare/source-control audit/start/occupied-port recovery/bounded smoke/strict-AI-smoke/advisory-AI/evidence/safety coverage, exact missing quickstart signal reporting in bundle paths, persisted blocker-count/ID/list agreement, completion-audit complete/blocker-list consistency, completion-audit blocked-item/list mirror enforcement, completion-audit blocker-ID mirror enforcement, plug-and-play fresh-clone summary drift enforcement, fresh-clone source-control HEAD summary preservation, fresh-clone strict-AI smoke fail-closed validation, and recovery-status stale source-control HEAD blocking were added; lint, dead-code, shellcheck, and gbrain skipped because those tools are not installed/detected; history is tracked under `~/.gstack/projects/*/health-history.jsonl` and marked stale when older than acceptance or missing a parseable timestamp |
| gstack review/planning workflow | Pass with local Git metadata | Git metadata is available from the software workspace at `../.git`; workflow status records that path for review evidence while still relying on the local audit chain instead of claiming an external pre-landing review |
| `npm run qa:gstack` | Pass clean-start browser/API QA | Loaded the built operator console with a timestamped clean `SEEKR_DATA_DIR=.tmp/qa-clean-gstack-*` local data directory; `/api/verify` returned `ok: true` with no errors, desktop and 375x812 screenshots were captured under `.gstack/qa-reports/screenshots/`, `/api/session` read back the current acceptance checksum with `commandUploadEnabled: false`, and the report remains browser/API QA only |
| `npm run rehearsal:evidence -- --label local-smoke` | Pass with expected local smoke warnings | Wrote `.tmp/rehearsal-evidence/seekr-rehearsal-evidence-local-smoke-2026-05-09T17-53-30-552Z.{json,md}` with `commandUploadEnabled: false`; warnings noted acceptance was from an earlier boot and no real source adapters were feeding events |
| `npm run rehearsal:note -- --label alpha-readiness-template` | Pass, template only | Wrote `.tmp/rehearsal-notes/seekr-rehearsal-note-alpha-readiness-template-*.{json,md}` with `freshOperatorCompleted: false`, `commandUploadEnabled: false`, and warnings for missing before/after snapshots plus non-actual Jetson/Pi validation |
| `npm run audit:completion` | Pass local alpha, incomplete real-world status | Latest run wrote `.tmp/completion-audit/seekr-completion-audit-*.{json,md}` and reports `localAlphaOk: true`, `complete: false`, 8 real-world blockers with separate Jetson Orin Nano and Raspberry Pi 5 items, machine-readable blocker IDs, API-probe evidence match, and `commandUploadEnabled: false` |
| `npm run demo:package -- --label internal-alpha` | Pass local alpha handoff | Writes `.tmp/demo-readiness/seekr-demo-readiness-internal-alpha-*.{json,md}` with release, command-boundary scan, API-probe, and source-control handoff pointers, false hardware claims, current real-world blocker list, overnight status, source-control-aware DX perspective review, and next-evidence guidance for each blocked item |
| `npm run bench:evidence:packet -- --label jetson-bench` | Pass bench prep handoff | Writes `.tmp/bench-evidence-packet/seekr-bench-evidence-packet-jetson-bench-*.{json,md}` with 8 ordered task cards, false authorization fields, separate Jetson/Pi hardware archive cards, and no hardware-validation claim |
| `npm run handoff:index -- --label internal-alpha` | Pass local alpha handoff index | Writes `.tmp/handoff-index/seekr-handoff-index-internal-alpha-*.{json,md}` with 10 passing chain checks, acceptance/demo-to-safety-scan and API-probe consistency, API-probe acceptance timestamp/command-count/exact strict-AI readback, linked-artifact SHA-256 digests, no warnings, no blockers, false hardware claims, and `commandUploadEnabled: false` |
| `npm run test -- handoffIndex acceptanceScripts` | Pass | 2 test files, 15 tests passed, covering handoff-index API-probe acceptance timestamp, command-count, exact strict-AI scenario readback, acceptance release checksum path/metadata freshness, linked-artifact digest capture, stale pointer blocking, unsafe bench packet blocking, and docs/package command guards |
| `npm run handoff:verify` | Pass handoff digest verification | Writes `.tmp/handoff-index/seekr-handoff-verification-*.{json,md}` and verifies that the latest handoff index's linked artifacts still match their recorded byte counts and SHA-256 digests |
| `npm run audit:gstack` | Pass with workspace limitation | Writes `.tmp/gstack-workflow-status/seekr-gstack-workflow-status-*.{json,md}` with 4 workflows, 5 perspectives, health-history path/freshness, local gstack QA report status/path, `gstackAvailable: true`, explicit `gstackCliAvailable`, `gitMetadataPath: "../.git"`, `commandUploadEnabled: false`, and review workflow evidence pointing at parent Git metadata |
| `npm run handoff:bundle -- --label internal-alpha` | Pass local alpha review bundle | Writes `.tmp/handoff-bundles/seekr-handoff-bundle-internal-alpha-*` plus JSON/Markdown manifests after verifying the handoff digest table; requires unavailable-CLI gstack helper-tool root/count/name evidence, requires local AI prepare freshness against acceptance, copies the latest gstack workflow-status, TODO audit, source-control handoff, plug-and-play setup, local AI prepare JSON/Markdown, operator-start plug-and-play doctor JSON/Markdown, exact-row rehearsal-start smoke JSON/Markdown, strict local AI smoke status, and operator quickstart with the handoff artifacts; ready source-control handoff evidence must be no older than acceptance, and the bundle manifest surfaces local branch, local HEAD, remote default SHA, blocked/warning check counts, `workingTreeClean`, and clean-worktree status-line count for review; the copied operator-start doctor must reference the packaged source-control handoff, rehearsal-start smoke must preserve wrapper-generated setup/local-AI/source-control/smoke-doctor artifact pointers, strict AI smoke proof must match acceptance and preserve loopback Ollama URL plus per-case plan-kind/validator/unsafe-text/mutation safety fields, copied files retain SHA-256 digests, false hardware claims, false authorization fields, and `commandUploadEnabled: false` |
| `npm run handoff:bundle:verify` | Pass local alpha review-bundle verification | Writes `.tmp/handoff-bundles/seekr-review-bundle-verification-*.{json,md}` and verifies that copied review-bundle files still match the bundle manifest byte counts and SHA-256 digests, copied acceptance includes the exact required strict local AI scenario names and loopback Ollama URL, copied strict local AI smoke status has exact Ollama scenario order, acceptance-matched timestamp/model/loopback URL, per-case plan-kind, validator pass, no unsafe operator-facing text, and no thinking-time mutation, the copied API probe reads back the same acceptance timestamp, command count, scenario names, loopback Ollama URL, release checksum, command-boundary scan, and disabled command upload, the copied gstack workflow status including unavailable-CLI helper-tool evidence, referenced gstack QA report content/screenshots, TODO audit exact blocker-category order/top-level counts/well-formed support evidence, source-control handoff repository-safety/publication-state semantics plus ready-artifact freshness against copied acceptance and bundle-manifest repository URL/configured-remotes/local-branch/default-branch/ref-count/blocked-check-count/warning-check-count/local/remote SHA and clean-worktree summary agreement, plug-and-play setup env/data/safety checks plus copied generatedAt/status surfacing, local AI prepare model-prep freshness and semantics, operator-start plug-and-play doctor repository-safety/source-control-handoff/runtime dependency evidence plus matching packaged source-control handoff evidence, start-wrapper/AI/port/data/safety checks, freshness against copied acceptance, copied rehearsal-start smoke exact ordered setup/AI/source-control/smoke-doctor/startup/source-health/readiness/shutdown rows plus artifact pointers, and copied operator quickstart GitHub clone/software-directory setup/ai:prepare/source-control audit/start/strict-AI-smoke/advisory-AI/API/source-health/safety guidance including no AI-created command payloads or validation bypass are semantically valid/included, every copied file was scanned, and no high-confidence secret-pattern findings exist |
| `npm run test -- handoffBundle acceptanceScripts` | Pass | 2 test files, 114 tests passed, covering review-bundle top-level fresh-clone local HEAD, clone HEAD, source-control HEAD publication, plug-and-play setup generatedAt/status surfacing, copied-bundle verification blocking when those summaries drift from copied source artifacts, and docs/package command guards |
| `npm run test -- apiContracts gstackHealthHistory acceptanceScripts goalAudit gstackWorkflowStatus` | Pass | 5 test files, 67 tests passed, covering parser-compatible `health:gstack` JSONL output, append-only health-history behavior, command-upload-enabled refusal, hermetic missing-acceptance API contract fixtures, package/docs command coverage, gstack workflow evidence, and goal-audit command-surface enforcement |
| `npm run test -- sourceControlHandoff handoffBundle plugAndPlayReadiness goalAudit acceptanceScripts` | Pass | 5 test files, 223 tests passed, covering source-control CLI branch/SHA summary fields, local-branch semantic validation, branch-aware review-bundle packaging/verification, plug-and-play readiness branch summaries, goal-audit branch drift blocking, strict local AI smoke status packaging, copied strict-AI smoke/API-probe semantic verification, source-control/setup/doctor/exact-row rehearsal-start/operator-quickstart semantics, docs/package command guards, and real-world blocker preservation |
| `npm run audit:todo` | Pass TODO/blocker consistency | Writes `.tmp/todo-audit/seekr-todo-audit-*.{json,md}` with 8 unchecked TODOs mapped to 8 exact ordered real-world blocker categories, top-level summary counts, the latest completion audit artifact matched, and `commandUploadEnabled: false` |
| `npm run test -- benchEvidencePacket demoReadinessPackage acceptanceScripts` | Pass | 3 test files, 21 tests passed, covering demo blocker normalization, source-control DX freshness, bench packet compatibility splitting for legacy combined target-board checklist items, and docs/package command guards |
| `npm run test -- goalAudit todoAudit completionAudit acceptanceScripts` | Pass | 4 test files, 63 tests passed, covering computed completion-audit blocker categories, TODO/blocker consistency, partial TODO category clearance, persisted goal-audit remaining-blocker count/ID/list agreement, goal-audit API readback-to-acceptance and AI-summary matching, goal-audit remaining-blocker derivation for partial target-board evidence, goal-audit stale plug-and-play readiness blocking, and package/docs command guards |
| `npm run test -- gstackWorkflowStatus handoffBundle goalAudit acceptanceScripts` | Pass | 4 test files, 153 tests passed, covering local gstack QA report validation, stale browser-QA freshness recording, failed browser-QA check row blocking in workflow and copied-bundle verification, missing browser-QA screenshot blocking, health-history `ts`/`timestamp` parsing plus unparseable timestamp handling, parent Git metadata path detection, required health-history metadata/path, required QA report metadata/path/screenshot paths when QA is marked pass or stale, manifest-level limitation preservation including unavailable gstack CLI details, unavailable-CLI helper-tool evidence preservation in bundle, copied-bundle, and goal audit paths, stale/missing health and QA limitation-detail preservation, exact workflow/perspective row preservation, perspective status/score/next-action preservation, required per-workflow installed-skill availability, top-level `pass-with-limitations` status for limitation-only evidence, Git review evidence or no-Git review limitations in workflow artifacts and review bundles, required TODO blocker-category/top-level-count/support-shape preservation in review bundles, gstack workflow-status packaging in review bundles, copied gstack QA report/screenshot inclusion, copied gstack semantic verification, copied setup/operator-doctor/operator-quickstart semantic blocking including advisory-AI command-safety guidance, repository-safety, and runtime dependency evidence preservation, critical-versus-soft doctor check status semantics, stale gstack and setup bundle blocking in goal audit, stale plug-and-play readiness blocking in goal audit, missing copied QA metadata blocking, bundle tamper detection, copied-bundle secret-scan blocking and coverage failure, docs/package command guards, and real-world blocker preservation |
| `npm run test -- plugAndPlayReadiness gstackWorkflowStatus handoffBundle goalAudit acceptanceScripts` | Pass | 5 test files, 199 tests passed, covering direct plug-and-play audit enforcement of current gstack workflow/perspective row order, current health/browser-QA evidence, unavailable-CLI helper-tool root/count/name preservation, review-bundle workflow packaging, copied workflow semantic verification, goal-audit freshness, docs/package command guards, and real-world blocker preservation |
| `npm run test -- localAiPrepare localSetup plugAndPlayDoctor plugAndPlayReadiness sourceControlHandoff operatorQuickstartContract acceptanceScripts goalAudit handoffBundle` | Pass | 9 test files, 258 tests passed, covering first-class local AI model-prep evidence, setup/doctor/rehearsal wrapper ordering, source-control fresh-clone guidance, operator quickstart model-prep guidance, review-bundle local AI prepare packaging/verification, plug-and-play readiness freshness, goal-audit enforcement, and docs/package command guards |
| `npm run test -- localAiPrepare plugAndPlayReadiness handoffBundle acceptanceScripts operatorQuickstartContract` | Pass | 5 test files, 187 tests passed, covering local AI prepare model-to-acceptance matching, plug-and-play readiness fail-closed behavior for mismatched prepared models, review-bundle creation and copied-bundle verification blocking mismatched local AI prepare evidence, operator quickstart coverage, and docs/package guards |
| `npm run test -- localAiPrepare plugAndPlayReadiness handoffBundle freshCloneOperatorSmoke acceptanceScripts` | Pass | 5 test files, 186 tests passed, covering local AI prepare rejection of non-Ollama pull-command evidence, relative lookalike executable paths, unsafe and shell-metacharacter model arguments, stale prepare artifacts, extra pull-argument or missing exact-command evidence, downstream plug-and-play readiness and review-bundle use of the shared local-AI validator, fresh-clone smoke local-AI artifact validation, and docs/package command guards |
| `npm run test -- acceptanceScripts` | Pass | 1 test file, 7 tests passed, including the test-matrix command-block guard that keeps `ai:prepare` between `setup:local` and `doctor` in the documented plug-and-play flow and keeps setup, AI prep, bounded startup smoke, and final doctor before review bundling |
| `npm run test -- acceptanceScripts plugAndPlayReadiness handoffBundle goalAudit` | Pass | 4 test files, 193 tests passed, covering docs/package command guards, plug-and-play readiness local AI prepare freshness checks, review-bundle local AI prepare packaging and verification, and goal-audit handoff/readiness enforcement |
| `npm run test -- localSetup plugAndPlayDoctor acceptanceScripts` | Pass | 3 test files, 24 tests passed, covering non-destructive `.env` setup, project-local rehearsal data preparation, outside-root env path blocking, missing local-AI env-default blocking, missing package runtime metadata, missing repository-safety policy blocking, missing runtime dependency and dev-server binary blocking, setup script coverage in doctor, and docs/package command guards |
| `npm run test -- envLoader plugAndPlayReadiness gstackBrowserQa acceptanceScripts apiContracts readiness goalAudit` | Pass | 9 test files, 87 tests passed, covering project-local `.env` loading, explicit environment precedence, outside-root env-file rejection, operator Ollama env defaults, advisory-AI operator quickstart coverage, command-boundary count reporting in browser QA evidence, package/docs command coverage, API config/session readback, readiness AI evidence, and goal-audit enforcement |
| `npm run test -- plugAndPlayReadiness` | Pass | 1 test file, 65 tests passed, covering local plug-and-play readiness, direct API readback-to-acceptance and exact required AI scenario-name plus loopback-Ollama-URL matching, untracked extra strict-AI scenario blocking, source-control handoff generatedAt freshness against acceptance, current gstack workflow/perspective row exactness, review-bundle source-control and setup generatedAt/status summary drift blocking, operator quickstart GitHub clone/software-directory setup/ai:prepare/source-control audit/start/occupied-port recovery/advisory-AI/evidence/safety coverage, exact ordered rehearsal-start smoke rows plus prerequisite artifact-pointer preservation, strict local AI smoke status pointer enforcement, persisted blocker-count/ID/list agreement, fail-closed completion-audit `complete`/summary/blocked-item/blocker-ID/blocker-list consistency, direct rejection of missing, stale, wrong-model, or unsafe local AI prepare artifacts, operator-start doctor exact-row artifact semantics including extra/reordered row blocking, soft doctor warning surfacing including auto-recoverable default-port fallback, stale review-bundle blocking, healthy occupied-port source/test contract coverage, and real-world blocker preservation |
| `npm run test -- plugAndPlayReadiness acceptanceScripts` | Pass | 2 test files, 72 tests passed, covering plug-and-play readiness setup-summary agreement, docs/package command guards, and real-world blocker preservation |
| `npm run test -- goalAudit plugAndPlayReadiness handoffBundle` | Pass | 3 test files, 228 tests passed, covering top-level goal-audit enforcement that plug-and-play readiness review-bundle setup path/generatedAt/status summaries match the latest bundle verification, plus plug-and-play readiness and handoff-bundle contracts |
| `npm run test -- plugAndPlayReadiness goalAudit acceptanceScripts` | Pass | 3 test files, 128 tests passed, covering plug-and-play readiness publication of review-bundle local-AI-prepare, operator-start doctor, rehearsal-start smoke, fresh-clone smoke, strict-AI-smoke, and operator-quickstart proof paths, goal-audit drift blocking for those summaries, and docs/package command guards |
| `npm run test -- plugAndPlayReadiness handoffBundle goalAudit` | Pass | 3 test files, 207 tests passed, covering rehearsal-start smoke setup/AI/source-control/smoke-doctor artifact pointers, exact smoke row enforcement, review-bundle packaging and verification, plug-and-play readiness, goal-audit handoff chain enforcement, and real-world blocker preservation |
| `npm run test -- goalAudit plugAndPlayReadiness` | Pass | 2 test files, 100 tests passed, covering goal-audit enforcement that plug-and-play readiness preserves the exact current completion-audit blocker list, plus plug-and-play fail-closed blocker summary/list consistency |
| `npm run test -- plugAndPlayReadiness plugAndPlayArtifactContract plugAndPlayDoctor` | Pass | 3 test files, 72 tests passed, covering direct plug-and-play audit reuse of the shared exact-row operator-start doctor artifact contract, extra/reordered doctor-row blocking, local startup doctor preflight, and plug-and-play artifact contract guards |
| `npm run test -- plugAndPlayDoctor plugAndPlayReadiness` | Pass | 2 test files, 63 tests passed, covering local startup doctor preflight, package runtime metadata enforcement, missing repository-safety policy blocking, missing runtime dependency and dev-server binary blocking, runtime dependency evidence preservation, repository-safety row preservation, start-wrapper contract blocking inside doctor, setup-before-ai-prepare-before-source-control-before-doctor-before-dev ordering, stale source-control artifact refresh before doctor, Ollama model availability blocking, auto-recoverable unconfigured default-port conflicts, explicit occupied-port warnings, healthy occupied-port source/test contract coverage, unsafe environment flag blocking, operator-start doctor artifact generation, rehearsal-start wrapper defaults, advisory-AI operator quickstart coverage, and plug-and-play readiness enforcement of the latest operator-start doctor artifact rows and critical-versus-soft statuses |
| `npm run test -- plugAndPlayDoctor plugAndPlayReadiness acceptanceScripts goalAudit` | Pass | 4 test files, 74 tests passed, covering local startup doctor preflight, package runtime metadata enforcement, repository-safety doctor enforcement, plug-and-play readiness enforcement of the latest doctor runtime dependency/repository-safety evidence/start-wrapper/AI/port/data/safety rows/statuses including healthy occupied-port source/test coverage, advisory-AI operator quickstart coverage, and setup-before-ai-prepare-before-source-control-before-doctor-before-dev rehearsal-start wrapper, package/docs command coverage, and goal-audit enforcement |
| `npm run test -- plugAndPlayReadiness goalAudit handoffBundle acceptanceScripts` | Pass | 4 test files, 217 tests passed, covering plug-and-play setup, local AI prepare, operator-start doctor, operator quickstart, source-control handoff, strict local AI smoke status, and bounded rehearsal-start smoke packaging in handoff bundles, copied setup/local-AI-prepare/operator-doctor/operator-quickstart/source-control/strict-AI semantic verification, advisory-AI command-safety guidance, repository-safety, runtime dependency evidence preservation, start-wrapper check preservation, critical-versus-soft doctor check status semantics, plug-and-play readiness artifact row/status/evidence semantics, stale setup/local-AI-prepare/operator-doctor/operator-quickstart/source-control/smoke/strict-AI bundle pointer blocking in readiness and goal-audit paths, rehearsal-start smoke prerequisite artifact-pointer preservation, rehearsal-start wrapper enforcement, package/docs command coverage, and real-world blocker preservation |
| `npm run test -- handoffBundle plugAndPlayReadiness localSetup acceptanceScripts` | Pass | 4 test files, 101 tests passed, covering setup artifact packaging in review bundles, copied setup semantic verification, copied doctor repository-safety/runtime dependency evidence preservation, copied operator quickstart advisory-AI command-safety verification, stale setup bundle blocking in plug-and-play readiness, operator quickstart coverage, non-destructive local setup, package/docs command coverage, and setup/operator-doctor readiness guards |
| `npm run test -- plugAndPlayReadiness envLoader acceptanceScripts goalAudit` | Pass | 4 test files, 64 tests passed, covering plug-and-play audit enforcement of env-loader implementation/wiring/tests, project-local `.env` behavior, rehearsal-start wrapper defaults and setup-before-ai-prepare-before-source-control-before-doctor-before-dev ordering, advisory-AI operator quickstart coverage, operator-start doctor artifact row/status/evidence semantics including runtime dependencies, healthy occupied-port source/test contract coverage, package/docs command coverage, and goal-audit enforcement |
| `npm run test -- plugAndPlayReadiness acceptanceScripts goalAudit` | Pass | 3 test files, 98 tests passed, covering local plug-and-play readiness, direct API readback-to-acceptance, exact required strict-AI scenario matching, untracked extra strict-AI scenario blocking, advisory-AI operator quickstart coverage, persisted blocker-count/ID/list agreement, completion-audit complete/blocker-list consistency, operator Ollama environment-default enforcement, rehearsal-start wrapper defaults and setup-before-ai-prepare-before-source-control-before-doctor-before-dev ordering, operator-start doctor artifact row/status/evidence semantics including runtime dependencies, soft warning surfacing, stale review-bundle blocking, package/docs command coverage, and goal-audit enforcement of the plug-and-play readiness artifact |
| `npm run test -- handoffBundle operatorQuickstartContract acceptanceScripts` | Pass | 3 test files, 114 tests passed, covering exact missing operator quickstart signal reporting in bundle creation and bundle verification, occupied-port recovery quickstart guidance, and docs/package command guards |
| `npm run test -- plugAndPlayDoctor plugAndPlayReadiness handoffBundle goalAudit acceptanceScripts` | Pass | 5 test files, 159 tests passed, covering source-control handoff warning-state blocking, repository-safety/runtime operator-start doctor evidence including runtime `data/` ignore coverage, soft doctor warnings, advisory-AI operator quickstart coverage, copied-bundle doctor/operator-quickstart semantics, package/docs command coverage, and goal-audit enforcement |
| `npm run test -- plugAndPlayDoctor plugAndPlayReadiness handoffBundle acceptanceScripts` | Pass | 4 test files, 157 tests passed, covering operator-start versus rehearsal-start-smoke doctor profile separation, smoke doctor artifacts not superseding operator-start doctor evidence in readiness or handoff bundles, read-only listener diagnostics for occupied non-SEEKR ports, published local HEAD and clean-worktree source-control details in doctor/readiness artifacts, copied source-control semantic verification, and docs/package command guards |
| `npm run test -- operatorQuickstartContract plugAndPlayReadiness handoffBundle sourceControlHandoff acceptanceScripts goalAudit` | Pass | 6 test files, 224 tests passed, covering the shared operator quickstart contract directly including GitHub clone/software directory entry, occupied-port recovery, `ollama pull llama3.2` model-prep guidance, and strict local AI smoke proof guidance, plus plug-and-play readiness, source-control handoff, bundle creation and verification surfacing exact missing quickstart signals, goal-audit enforcement, and docs/package command guards |
| `npm run test -- plugAndPlayArtifactContract plugAndPlayDoctor plugAndPlayReadiness handoffBundle goalAudit acceptanceScripts` | Pass | 6 test files, 260 tests passed, covering exact ordered plug-and-play setup/operator-start doctor artifact contracts, operator doctor source-control evidence matching against the packaged source-control handoff, per-occupied-port listener diagnostics, copied PID evidence, and fallback evidence, exact printed non-default fallback-port proof, and plug-and-play operator guidance enforcement for auto-recoverable port conflicts, plug-and-play doctor/readiness, bundle creation, bundle verification, goal audit, and docs/package command guards |
| `npm run test -- plugAndPlayDoctor plugAndPlayReadiness plugAndPlayArtifactContract operatorQuickstartContract handoffBundle acceptanceScripts` | Pass | 6 test files, 191 tests passed, covering shared doctor artifact contract rejection of non-SEEKR port warnings without listener diagnostics, auto-recoverable unconfigured default-port conflicts, explicit occupied-port warnings, doctor/readiness/bundle contract checks, operator quickstart contract coverage, and docs/package command guards |
| `npm run test -- plugAndPlayDoctor plugAndPlayArtifactContract plugAndPlayReadiness acceptanceScripts` | Pass | 4 test files, 83 tests passed, covering healthy already-running SEEKR port recognition in the startup doctor, plug-and-play readiness enforcement that the doctor source/test contract preserves that recognition and ready source-control handoff evidence follows acceptance, shared plug-and-play artifact contracts, plug-and-play readiness, and docs/package command guards |
| `npm run test -- sourceControlHandoff plugAndPlayReadiness plugAndPlayDoctor acceptanceScripts` | Pass | 4 test files, 100 tests passed, covering read-only GitHub handoff audit generation, missing local Git/remote refs blocking, publication next-action checklist persistence, `git ls-remote` warning preservation, warning-only remote-ref source-control artifacts remaining soft doctor and plug-and-play readiness warnings, plug-and-play source-control warnings, doctor script coverage, and docs/package command guards |
| `npm run test -- handoffBundle plugAndPlayReadiness sourceControlHandoff acceptanceScripts` | Pass | 4 test files, 179 tests passed, covering source-control blocked/warning check counts in review-bundle and plug-and-play readiness summaries, warning-only remote-ref handoff packaging and copied-bundle verification, plug-and-play readiness source-control warning behavior, shared source-control semantic validation, and docs/package command guards |
| `npm run test -- goalAudit plugAndPlayReadiness handoffBundle sourceControlHandoff acceptanceScripts` | Pass | 5 test files, 225 tests passed, covering top-level goal-audit drift blocking for source-control blocked/warning check counts in plug-and-play readiness and review-bundle summaries, copied-bundle/source-control semantic verification, plug-and-play readiness source-control warning behavior, shared source-control semantic validation, and docs/package command guards |
| `npm run test -- handoffBundle plugAndPlayReadiness goalAudit sourceControlHandoff acceptanceScripts` | Pass | 5 test files, 199 tests passed, covering source-control handoff packaging in review bundles, copied source-control semantic verification including publication next-action checklist shape, stale source-control bundle blocking in plug-and-play readiness and goal audit, read-only GitHub handoff audit generation, advisory-AI operator quickstart semantic enforcement, non-negated disabled command-upload/hardware-actuation landing wording, docs/package command guards, and real-world blocker preservation |
| `npm run test -- sourceControlHandoff plugAndPlayDoctor plugAndPlayReadiness handoffBundle demoReadinessPackage goalAudit acceptanceScripts` | Pass | 7 test files, 244 tests passed, covering machine-readable `workingTreeClean` source-control handoff evidence, shared source-control handoff semantic validation, exact ordered source-control check-ID rejection, local HEAD publication and clean-worktree gates, persisted blocked/warning check counts, publication next-action checklist validation, count-mismatch rejection, unsafe source-control artifact blocking in doctor/readiness/bundle verification, demo DX source-control review, source-control warning preservation, review-bundle source-control packaging, docs/package command guards, and real-world blocker preservation |
| `npm run test -- sourceControlHandoff plugAndPlayDoctor plugAndPlayReadiness handoffBundle goalAudit` | Pass | 5 test files, 212 tests passed, covering persisted source-control blocked/warning check counts, publication next-action checklist validation, count-mismatch rejection, operator quickstart source-control audit and advisory-AI command-safety enforcement in readiness and bundle verification, source-control warning preservation, review-bundle source-control packaging, and real-world blocker preservation |
| `npm run test -- goalAudit` | Pass | 1 test file, 54 tests passed, including goal-audit enforcement that direct API readback and exact required AI scenario-name plus loopback-Ollama-URL evidence match acceptance, untracked extra strict-AI scenarios fail local alpha, review-bundle verification points at strict local AI smoke status, plug-and-play readiness references the latest API probe, source-control handoff, and `docs/OPERATOR_QUICKSTART.md` evidence, is current relative to acceptance, directly rejects stale, unsafe, or wrong-model local AI prepare evidence, surfaces plug-and-play warning details, and rejects stale plug-and-play source-control and review-bundle repository URL/configured-remotes/local-branch/default-branch/ref-count/SHA/clean-worktree summary fields |
| `npm run test -- plugAndPlayDoctor plugAndPlayReadiness sourceControlHandoff acceptanceScripts` | Pass | 4 test files, 100 tests passed, covering source-control count persistence, rehearsal-start source-control refresh before doctor, warning-only remote-ref source-control artifacts remaining soft doctor and plug-and-play readiness warnings, source-control warning preservation, and docs/package command guards |
| `npm run smoke:rehearsal:start` | Pass | Reproduced the acceptance failure path after source-control count hardening; wrapper now refreshes source-control handoff before doctor, starts API/client on temporary ports, checks config/source-health/readiness, keeps `commandUploadEnabled: false`, and shuts down cleanly |
| `npm run test -- plugAndPlayReadiness handoffBundle` | Pass | 2 test files, 168 tests passed, covering exact ordered rehearsal-start smoke setup/AI/source-control/smoke-doctor/startup/source-health/readiness/shutdown rows, post-acceptance freshness for rehearsal-start smoke, fresh-clone smoke, handoff bundle, and bundle verification evidence, prerequisite artifact-pointer preservation, and existing setup/doctor/source-control/operator quickstart review-bundle semantics |
| `npm run test -- goalAudit plugAndPlayReadiness handoffBundle acceptanceScripts` | Pass | 4 test files, 230 tests passed, covering top-level goal-audit rejection of stale rehearsal-start smoke, fresh-clone smoke, handoff bundle, and bundle verification evidence, plug-and-play readiness, review-bundle semantics, and docs/package command guards |
| `npm run test -- plugAndPlayReadiness goalAudit handoffBundle acceptanceScripts` | Pass | 4 test files, 217 tests passed, covering bounded rehearsal-start smoke artifact validation, setup/AI/source-control/smoke-doctor artifact-pointer preservation, review-bundle smoke and strict local AI smoke status packaging/verification, advisory-AI operator quickstart semantic enforcement, stale smoke and missing strict-AI bundle pointer blocking, package/docs command coverage, and goal-audit smoke/strict-AI chain enforcement |
| `npm run test -- operatorQuickstartContract acceptanceScripts` | Pass | 2 test files, 24 tests passed, covering the GitHub landing README fresh-clone path, operator quickstart contract including local Ollama model preparation, and docs/package command guards |
| `npm run test -- sourceControlHandoff demoReadinessPackage plugAndPlayDoctor plugAndPlayReadiness handoffBundle goalAudit acceptanceScripts` | Pass | 7 test files, 228 tests passed, covering the source-control GitHub landing README fresh-clone check, fenced-shell command-line final bounded-smoke/strict-AI/plug-and-play audit proof command ordering, non-negated explicit disabled command-upload/hardware-actuation landing wording, downstream rejection of `github-landing-readme` pass artifacts missing ordered-command or final AI/readiness evidence markers, shallow fresh-clone smoke check for startup-critical package/env/start-wrapper/operator files, cloned landing README contract proof, shared operator-quickstart contract proof in fresh-clone evidence, `npm ci --dry-run` install-consistency blocking, invalid published landing README/operator quickstart blocking, repair next-action, shared source-control semantic validation including rejection of `fresh-clone-smoke` pass artifacts with missing or non-exact clone/install/startup-file evidence plus ready artifacts with mismatched local/remote SHAs, missing default branch, missing refs, or non-SEEKR remotes, downstream doctor/readiness/bundle/goal/demo source-control guards, and docs/package command guards |
| `npm run test -- sourceControlHandoff operatorQuickstartContract demoReadinessPackage plugAndPlayDoctor plugAndPlayReadiness handoffBundle acceptanceScripts` | Pass | 7 test files, 206 tests passed, covering source-control fresh-clone landing README and operator quickstart contract proof, GitHub landing README fenced-shell command-line bounded-smoke before strict-AI before plug-and-play audit guidance plus non-negated explicit disabled command-upload/hardware-actuation wording and contradictory enabled/allowed authority rejection, demo DX source-control review, downstream doctor/readiness/bundle source-control semantic guards, and docs/package command guards |
| `npm run test -- sourceControlHandoff` | Pass | 1 test file, 20 tests passed, covering ordered GitHub landing README source-control audit blocking, root landing README fenced-shell command-line bounded-smoke/final-doctor/strict-AI/plug-and-play audit proof guidance, missing final doctor rerun rejection, non-negated explicit disabled command-upload/hardware-actuation wording, prose-only and shell-comment command mention rejection, fresh-clone landing README/operator quickstart contract proof, and source-control semantic validation |
| `npm run test -- sourceControlHandoff operatorQuickstartContract acceptanceScripts` | Pass | 3 test files, 45 tests passed, covering ordered GitHub landing README source-control audit blocking, root landing README fenced-shell command-line bounded-smoke/final-doctor/strict-AI/plug-and-play audit proof guidance, non-negated explicit disabled command-upload/hardware-actuation wording, prose-only and shell-comment command mention rejection, fresh-clone landing README/operator quickstart contract proof, and docs/package command guards |
| `npm run test -- operatorQuickstartContract plugAndPlayReadiness handoffBundle acceptanceScripts` | Pass | 4 test files, 158 tests passed, covering the shared operator quickstart contract, install-before-setup/start command ordering, bounded rehearsal-start smoke proof guidance and ordering, plug-and-play readiness quickstart enforcement, copied-bundle quickstart semantic verification, and docs/package command guards |
| `npm run setup:local` | Pass local setup | Writes `.tmp/plug-and-play-setup/seekr-local-setup-*.{json,md}`, creates `.env` only if missing, prepares `.tmp/rehearsal-data`, and keeps `commandUploadEnabled: false` |
| `npm run audit:source-control` | Pass audit generation, publication state checked | Writes `.tmp/source-control-handoff/seekr-source-control-handoff-*.{json,md}` and CLI JSON with `commandUploadEnabled: false`, ordered GitHub landing README fresh-clone guidance through bounded smoke, final standalone doctor rerun, strict local AI smoke, plug-and-play audit proof commands, and explicit disabled command-upload/hardware-actuation wording, shallow fresh-clone smoke proof for startup-critical package/env/start-wrapper/operator files, cloned landing README contract proof, cloned HEAD evidence, `npm ci --dry-run` install consistency, checked startup-file count, local Git metadata path, local branch, GitHub default-branch SHA, local HEAD SHA, `workingTreeClean: true`, zero clean-worktree status lines, and persisted blocked/warning check counts; ready handoff requires ordered root landing guidance, final AI/readiness proof guidance, disabled authority wording, shallow fresh-clone contents, cloned landing README/operator quickstart contract proof, install consistency, source-control fresh-clone HEAD to match the GitHub default branch, local HEAD to match the GitHub default branch, and machine-readable clean-worktree evidence |
| `npm run test -- sourceControlHandoff freshCloneOperatorSmoke plugAndPlayDoctor demoReadinessPackage handoffBundle plugAndPlayReadiness goalAudit acceptanceScripts` | Pass | 8 test files, 294 tests passed, covering the shared source-control fresh-clone startup-file contract, fresh-clone operator smoke preservation of source-control fresh-clone HEAD/dry-run/path-count summaries and strict local AI smoke summaries, doctor/demo/readiness fixture compatibility, handoff-bundle and bundle-verification preservation, plug-and-play readiness summary agreement, goal-audit drift blocking, docs/package command guards, and real-world blocker preservation |
| `npm run test -- plugAndPlayReadiness handoffBundle goalAudit plugAndPlayArtifactContract acceptanceScripts` | Pass | 5 test files, 253 tests passed, covering setup artifact freshness against acceptance in plug-and-play readiness, bundle creation, bundle verification, and goal audit plus shared setup/doctor artifact contracts and docs/package command guards |
| `npm run audit:plug-and-play` | Pass local plug-and-play readiness, incomplete real-world status | Writes `.tmp/plug-and-play-readiness/seekr-plug-and-play-readiness-*.{json,md}` with local app, local Ollama AI, API readback matched to the latest acceptance timestamp, local-AI summary including strict smoke scenario names, loopback Ollama URL, release checksum, and command-boundary scan summaries, QA, review-bundle, latest warning-free source-control handoff artifact with ready-artifact generatedAt freshness against acceptance, top-level source-control and operator-start port summaries including default-port occupancy, auto-recoverability, listener diagnostics, and current fallback port candidates, top-level fresh-clone operator smoke summary with repository, local/clone HEAD, source-control local/remote-default HEAD, local AI model, fresh-clone strict-AI smoke path/provider/model/loopback URL/case count, downstream artifact statuses, exact checked rows, and generatedAt freshness against acceptance, latest setup artifact plus verified-bundle setup generatedAt/status summary agreement, latest local AI prepare artifact with model-to-acceptance matching, generatedAt freshness against acceptance, and shared semantic validation that rejects unsafe/malformed model-prep evidence, latest operator-start doctor preflight that references the latest source-control handoff, latest rehearsal-start smoke artifact with generatedAt freshness against acceptance, current handoff bundle and bundle verification freshness against acceptance, operator quickstart advisory-AI/no-command-payload/no-validation-bypass coverage, required setup/doctor repository-safety/source-control-handoff/runtime dependency evidence/start-wrapper/AI/port/data/safety rows/statuses with explicit soft doctor warnings surfaced while auto-recoverable default-port conflicts can pass, rehearsal-start wrapper, source-control handoff warning-state blocking, current gstack workflow installed-skill/CLI/helper-tool evidence, current verified-bundle source-control, setup summary, smoke, strict local AI smoke status pointer checks, and completion-audit `complete`/summary.blocked/blocked-item/blocker-ID/blocker-list consistency, while preserving real-world blockers |
| `npm run audit:goal` | Pass local alpha objective audit, incomplete real-world status | Writes `.tmp/goal-audit/seekr-goal-audit-*.{json,md}` with named-file, named-command, safety-boundary, acceptance, API-probe, completion-audit, demo/handoff/bundle/gstack/source-control, direct local AI prepare freshness/model validation, plug-and-play readiness including fresh-clone source-control HEAD and strict-AI smoke summary agreement, TODO/blocker consistency, docs/goal, persisted remaining-blocker count/ID/list, and real-world-blocker checklist items; reports `complete: false` until physical evidence exists |
| `npm run smoke:preview` | Pass | Built production assets and checked static shell, built asset, config, readiness |
| `npm run test:ui` | Pass | 6 Playwright tests passed, including field-laptop viewports |
| `npm run test -- readiness aiTools aiBoundaryLifecycle acceptanceScripts plugAndPlayReadiness` | Pass | 7 test files, 83 tests passed, covering strict AI smoke required-scenario validation, advisory AI tools, command-boundary lifecycle safety, docs/package command guards, and plug-and-play readiness |
| `npm run test -- acceptanceEvidence apiContracts apiProbe plugAndPlayReadiness goalAudit acceptanceScripts` | Pass | 6 test files, 92 tests passed, covering strict AI smoke scenario-name and loopback-Ollama-URL persistence through acceptance status, `/api/session`, API probe, plug-and-play readiness, and goal audit, plus docs/package command guards |
| `npm run test -- readiness acceptanceEvidence acceptanceScripts` | Pass | 6 test files, 72 tests passed, covering strict AI smoke loopback-Ollama-URL, per-case plan-kind, validator, unsafe operator-facing text, and mutation evidence plus acceptance/docs command guards |
| `npm run test -- readiness handoffBundle plugAndPlayReadiness goalAudit handoffIndex apiProbe acceptanceEvidence apiContracts` | Pass | 10 test files, 267 tests passed, covering strict local AI `commandUploadEnabled: false` persistence through readiness, acceptance, API readback, handoff index, review bundle creation/verification, plug-and-play readiness, and goal audit |
| `npm run test -- acceptanceScripts operatorQuickstartContract` | Pass | 2 test files, 25 tests passed, covering docs/package command guards and operator quickstart strict local AI proof guidance |
| `npm run test:ai:local` | Pass | Loopback Ollama URL, `llama3.2:latest`, `commandUploadEnabled: false`, 4 named advisory cases including detection/spatial prompt-injection scenarios, validator-approved plan kinds, no unsafe operator-facing text, and no mutation while thinking |
| `npm run release:checksum && npm run acceptance:record` | Pass | Writes the current file-set checksum under `.tmp/release-evidence/seekr-release-0.2.0-*.{json,sha256,md}` and records `.tmp/acceptance-status.json` with strict local AI status, required scenario names, loopback Ollama URL, and `commandUploadEnabled: false` |
| `npm run test -- completionAudit plugAndPlayReadiness goalAudit todoAudit acceptanceScripts` | Pass | 5 test files, 134 tests passed, covering completion-audit real-world blocker IDs, plug-and-play fail-closed blocker ID/details/list mirror checks, goal-audit blocker-ID drift blocking, TODO audit typed fixtures, and docs/package command guards |
| `npm run test -- freshCloneOperatorSmoke plugAndPlayReadiness goalAudit acceptanceScripts` | Pass | 4 test files, 134 tests passed, covering fresh-clone source-control HEAD summary preservation, strict local AI smoke execution and fail-closed evidence validation in fresh clones, plug-and-play readiness source-control/strict-AI summary publication, goal-audit drift blocking for those summaries, and docs/package command guards |
| `npm run test -- plugAndPlayReadiness goalAudit acceptanceScripts` | Pass | 3 test files, 121 tests passed, covering plug-and-play readiness operator-start port and fresh-clone summaries, direct local AI prepare missing/stale/wrong-model/unsafe blocking, goal-audit enforcement that default-port occupancy, auto-recoverable fallback, listener diagnostics, fresh-clone HEADs, source-control HEADs, and exact checked rows match the latest evidence, plus docs/package command guards |
| `npm run acceptance` | Pass | Check, edge, flight, SITL, SITL process IO, DimOS, command-boundary scan, strict local AI, UI, preview smoke, rehearsal-start smoke, release checksum, acceptance record, and API probe all passed; `.tmp/acceptance-status.json` carries the exact current release checksum, release file count, release byte count, 132 command-boundary scanned files, strict AI scenario names, loopback Ollama URL, strict AI `commandUploadEnabled: false`, and 13 recorded pre-record acceptance commands; latest full test pass was 58 test files / 575 tests |
| `SEEKR_OVERNIGHT_SECONDS=45 SEEKR_OVERNIGHT_SLEEP_SECONDS=1 bash scripts/overnight-loop.sh` | Pass | One bounded cycle refreshes `.tmp/overnight/STATUS.md` with check, UI, build, preview smoke, rehearsal-start smoke, release checksum, hardware probes, hardware archive, edge/flight/SITL/DimOS benches, local AI, acceptance record, final API probe after acceptance record, and unchecked TODOs from both `SEEKR_GCS_ALPHA_TODO.md` and `SEEKR_COMPLETION_PLAN.md` |
| `npm run probe:api` | Pass | Config, session acceptance, passing acceptance release/checksum and command-boundary summaries when present, readiness, hardware-readiness, source-health, verify, replays, malformed JSON, and `.tmp/api-probe/` JSON/Markdown evidence |
| `npm run probe:hardware` | Pass with off-board warnings | Jetson/Pi reports kept command upload disabled; current host is macOS, not target boards |
| `npm run probe:hardware:archive` | Pass, off-board readiness only | Wrote `.tmp/hardware-evidence/seekr-hardware-evidence-jetson-orin-nano+raspberry-pi-5-*.{json,md}` with `actualHardwareValidationComplete: false`, `hardwareValidationScope: "off-board-readiness"`, and `commandUploadEnabled: false` |
| `npm run acceptance` release/acceptance artifacts | Pass | Final acceptance status is `status: pass`, `currentBoot: true`, strict local AI `ollama`/`llama3.2:latest` with loopback Ollama URL and 4 required scenario names, and `commandUploadEnabled: false` |

The latest unattended overnight loop is `.tmp/overnight/STATUS.md`; final readback reports verdict `pass` and the current real-world blocker list.

## Real-World Blockers

- No completed fresh-operator field-laptop rehearsal closeout is present from an actual run.
- No actual Jetson Orin Nano hardware readiness archive is present from this machine.
- No actual Raspberry Pi 5 hardware readiness archive is present from this machine.
- No real read-only MAVLink serial/UDP bench connection has been validated.
- No real ROS 2 `/map`, pose, detection, LiDAR, or costmap topic bridge has been validated.
- No HIL failsafe run with manual override evidence has been archived.
- No Isaac Sim HIL capture from Jetson bench has been archived.
- No reviewed hardware-actuation policy exists, and none should exist until the above evidence is complete.
